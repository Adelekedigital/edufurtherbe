# Failure Modes — TEMPLATE

This project's incident log. Every row is something that actually went wrong
**here**, not a generic risk.

## Why this file earns its place

Generic standards encode what usually goes wrong. This file encodes what has gone
wrong in *this* codebase — a much better predictor of what will go wrong next,
and the only one of the two that cannot be downloaded.

Three properties make a row useful:

1. **How it surfaced** matters as much as what broke. A defect found by a user
   three weeks later is a different problem from one CI caught in ninety seconds,
   even when the code change is identical.
2. **The prevention must be mechanical where possible.** A rule that says "be
   careful" prevents nothing. A lint, a test, a constraint, or a function
   signature that makes the wrong call unwriteable does.
3. **Name the thing that lied.** Most expensive incidents involve something that
   *looked* correct — a green suite, a passing gate, a doc, a name. Record what
   gave false assurance; that is usually the reusable lesson.

## Adding a row

Add one whenever something is found the hard way — in review, in CI, in
production, or by a user. **Including near-misses.** A defect caught by luck
during review is the same defect; the only difference is where it stopped.

Write it the same day. The detail that makes a row useful is the first thing to
fade.

## The log

| Failure | How it surfaced | Prevention |
|---|---|---|
| The `pre_commit_guard.py` hook allowed `git commit --no-verify` — the exact command it exists to block. `json.load` rejects a byte-order mark, and the handler caught the parse error and returned 0, so any encoding quirk on stdin disabled the guard entirely | Never fired in anger. Found on the day it was written, by feeding it payloads directly instead of trusting that it was installed. It reported success on every single input, including the ones it was supposed to refuse | The guard now decodes `utf-8-sig` and, when the payload will not parse, scans the raw text rather than waving the command through — a malformed envelope can no longer switch it off. **A control that fails open is indistinguishable from no control, and it reports green either way.** Fail-open is correct for the formatter hook and wrong for anything whose job is to say no |
| The `pytest-unit` pre-commit hook passed under `uv run pre-commit run --all-files` and failed in the real git hook with `ModuleNotFoundError: No module named 'app'` | Only when the first genuine `git commit` was attempted. Every prior verification had been run through `uv run`, which put the project venv on PATH and hid it | Local hooks now invoke `uv run` explicitly instead of relying on `language: system` resolving against the caller's PATH. **Verify a hook the way it will actually be invoked.** `pre-commit run --all-files` under a wrapper is a different execution environment from `.git/hooks/pre-commit`, and only one of them is the one that matters |
| `core/config.py` silently ignored misspelled `EDUFURTHER_` environment variables, leaving defaults in place. `extra="forbid"` was assumed to cover it and does not — pydantic-settings drops env vars matching no field before validation ever sees them | Caught by a test written to assert the behaviour, which failed on first run. Had the test been written after the code, it would likely have been written to match what the code did | An explicit `model_validator` scans `os.environ` for unrecognised prefixed variables and refuses to start. **Watching the test fail is what found this** — the assertion encoded the intent, and the intent turned out not to be implemented. Matters more once payment secrets are configured this way |
| `project-conventions` and two ADRs asserted "roughly 700 members, 15 mentors, 5,000 mentoring minutes" as fact, and used those figures to justify decisions — the argument against dual-run, the case for hand-checking every mentor record, the scope of the PII in the gitignore rules. They were marketing numbers scraped from the public site. The database holds 1,200 users, 44 mentors and 1,073 session bookings — the mentor count was out by roughly 3x | Near-miss. Surfaced only when the legacy data model was finally shared, weeks of documents later. Nothing had failed; the figures were simply being quoted forward, and would next have been quoted into the mapping document | Row counts now come from `docs/bubble-data-model.md`, named as canonical in "Where the truth lives", and tier 2 says explicitly not to quote the public site. **A number in a settled-decisions table gets cited, not re-derived** — so its provenance matters as much as its value. Where a figure justifies a decision, name the source next to it. Still manual: nothing checks that a documented number matches reality |
| Every job in the Security workflow was broken, and nobody could have known, because the workflow had never run. The secret scan 403'd on `/pulls/{n}/commits` for want of `pull-requests: read`; `pip-audit --strict` died on our own editable package, which will never be on PyPI; and CodeQL analysed successfully then failed at upload because GitHub Advanced Security is unavailable on a private personal repo | The first time the workflows ever executed — on the first pull request of the project, minutes after the repo gained a remote | Permissions fixed; the dependency audit now runs against the exported lockfile with `--no-emit-project`, so our own package is excluded by construction rather than suppressed; CodeQL removed outright. **A workflow that has never run is not a passing workflow, it is an untested one.** The config had looked correct for as long as the repo existed |
| The obvious fix for the `pip-audit` failure was dropping `--strict`, which would have gone green instantly | Noticed while fixing it, not after | `--strict` is what converts "could not audit this dependency" from a silent pass into a failure. Removing it fixes the symptom by disabling the check for all 180-odd real dependencies too. **When the quickest way to green is deleting a flag, the flag is usually the thing doing the work** — the same reasoning applies to `continue-on-error`, `|| true`, and lowering a coverage floor |
| CodeQL was removed rather than guarded with `if:` or `continue-on-error` | Decision made while fixing the above | A skipped job and a job that swallows its exit code both render as a green check. Leaving it in would have restored the exact failure this file's worked example describes, on the same day that example was read. **Prefer an absent check you have documented to a present check that cannot fail** |
| pre-commit pinned ruff 0.7.4 / mypy 1.13.0 / bandit 1.7.10 while the venv resolved 0.16.1 / 2.3.0 / 1.9.4 — a major-version mypy gap, so the local gate and CI would have disagreed about identical code | Near-miss. Noticed while checking installed versions for an unrelated reason, before it produced a confusing "passes locally, fails in CI" report | Those three are pinned with `==` in `[dependency-groups] dev` and kept equal to the `rev:` values in `.pre-commit-config.yaml`, with a comment at each site saying so. Ruff's `extend-exclude` was aligned with the hooks' `exclude` for the same reason. Still manual — `pre-commit autoupdate` does not touch `pyproject.toml` |
| The shared `settings` fixture read whatever `.env` sat in the working directory, so any field it did not pin explicitly silently took that file's value. Its docstring claimed the opposite | Found while writing a test for it — but the *first* version of that test passed against the unfixed code, because it probed `debug`, which the fixture pins as an init argument. Init arguments outrank a dotenv value, so the two fields the fixture set were exactly the two that could never show the leak | The fixture passes `_env_file=None`. The test probes `cors_origins` — a field the fixture leaves alone — and fails with the leaked value visible. **A test that passes the moment you write it, against a bug you believe is there, is a broken probe rather than good news.** Watching it fail is what distinguishes the two, and the first failure to look for is the test's own |
| Squash-merging the base of a stacked PR with `--delete-branch` closed the dependent PR permanently. GitHub will not reopen a PR whose head commits have since been rewritten by the follow-up rebase, will not accept a `--base` change on a closed PR, and recreating the deleted base branch unlocks neither | Immediately, on the first merge of the initial three-PR stack: PR #2 went from `OPEN` to `CLOSED` the moment PR #1 merged. Its content survived on the branch and shipped as a replacement PR; the original number and review history did not | Merge the parent **without** `--delete-branch`; rebase each child with `git rebase --onto main <old-base>`; force-push with lease; retarget with `gh pr edit --base main`; delete a branch only once no open PR targets it. The rebase requirement had been identified and written down *before* the merge, and the branch was deleted anyway. **Predicting a consequence does not prevent it — only a step in the procedure does.** A risk named in prose and absent from the command you actually run is not mitigated |
| Every mentor's Google Calendar connection was revoked and calendar features were silently broken. The integration ran on Composio's **managed auth** — a shared OAuth application owned by the vendor, whose quota and publishing status are shared across all their customers and visible to none of them. Composio's own documentation directs users to supply their own credentials for any toolkit where end users see a consent screen, and names Google specifically | Found by opening the Composio dashboard during an unrelated stack discussion, with no way to tell how long the connections had been dead. Nothing alerted. No error surfaced anywhere anyone was watching. The integration had worked when it was built, and that was the last time anybody checked | Register and own the OAuth client, configured in Composio as a custom auth config (ADR 0004). **A vendor's zero-configuration default is a development path that resembles a production one** — it works on the day you build it, which is precisely what makes it convincing. Where a vendor offers to hold a credential on your behalf, establish whose account it belongs to before shipping, not after. Still missing: nothing monitors connection health, which is why the outage was measured in "whenever someone next looked" |
| `/adr-new` instructs copying the **MADR** template, while ADR 0001 — accepted, immutable — establishes **Nygard**, and 0001–0003 are all Nygard-shaped. Three new ADRs were about to be written in the wrong format, in a corpus whose whole value is consistency | Near-miss. Surfaced during a confidence check on a checklist, before any file was written. The checklist itself had said "MADR format, matching 0001–0003's existing structure" — a sentence that is self-contradictory and had been read several times without anyone noticing | Recorded as settled decision #19 in tier 2, which is where it survives; the command is deliberately **not** edited, because `.claude/commands/` ships with the tier-1 package (commit 51493f2) and the correction would be overwritten on the next update. **When a tier-1 asset and a tier-2 decision disagree, the tier-1 asset is the one that gets read first** — a slash command is more discoverable than a skill file, so the wrong answer is the more available one. Every future standards-package update can reintroduce this class of conflict; tier 2 is the only durable place to say which wins |
| A four-PR reconciliation plan was built on a directory listing that showed four files in `docs/adr/`. The directory held seven. ADRs 0004, 0005 and 0006 — all committed in `HEAD`, all dated that same day — were never opened. The plan therefore proposed creating "ADR 0004" over an existing record, proposed creating a `docs/adr/README.md` that already existed, proposed re-deciding the Nygard-vs-MADR question that settled decision #19 had already closed, and listed a row-count "conflict" that tier 2 and both ADR correction notes had already fixed | Near-miss, and it surfaced only on the *second* time the same question was asked. The first listing was simply believed. A `grep` run to verify an unrelated claim returned a path — `docs/adr/0005-data-platform.md` — that could not exist under the assumed state, which is what exposed it. Nothing had been written yet | State-gathering commands are re-run immediately before planning *and* immediately before the first edit, with their output quoted rather than recalled; that is now an explicit checklist step rather than an intention. **A stale read is indistinguishable from a fresh one once it is in your head** — a file listing carries no timestamp, and a long session ages every earlier observation silently. The second and third confidence checks each found defects the first had asserted away, which is an argument for asking again rather than for asking better. Still manual: nothing forces the re-read |
| The `format_on_edit.py` PostToolUse hook runs `ruff --fix` after **every** edit, so an import added in one edit and used in the next is deleted in between as F401. It happened three times in one sitting: `SecretStr` in `config.py` (twice, the second time while reverting a deliberate probe) and `to_async_dsn` in `migrations/env.py` | Twice as `PydanticUserError: Settings is not fully defined`, which names the symbol but not the reason and reads like a pydantic problem. Once as a plain `NameError` from inside alembic, at the bottom of eleven failing tests | Add the import and its first use in the **same** edit, or write the usage first. **`build-workflow`'s "imports first" rule is unsafe in this repository**, and the generic skill cannot know that because the hazard comes from a hook this project installs. The tier-1 rule is correct everywhere the toolchain does not rewrite files between steps; here it inverts |
| The `set_updated_at` trigger was given a guard — "stamp `now()` only if the caller did not change `updated_at`" — to stop a re-runnable importer overwriting Bubble's modified dates. The guard cannot work: an idempotent importer writes the value the row already holds, so "caller changed it" and "caller never mentioned it" are the same state | A hand-written SQL probe **passed** it, because the probe's second UPDATE moved the value (`now()` → 2019) while a real re-run writes 2019 over 2019. Only the pytest version, which followed the actual ETL sequence, failed | Guard removed; the trigger is unconditional and matches the source DDL. **The probe passed because it exercised a sequence the system will never perform** — a green check against invented inputs is weaker evidence than no check, because it retires the suspicion that motivated it. Reproduce the caller's real order of operations, not a convenient one. **Prevention superseded (M1, settled decision #29):** the fix recorded here was `legacy_created_at` / `legacy_modified_at` columns beside `legacy_bubble_id`. Those were withdrawn — Bubble's dates land in `created_at` / `updated_at` directly, and **the loader disables `trg_set_updated_at` for the duration of the import**. The hazard is unchanged and now sits in the loader rather than the schema, so M1c reconciliation asserts `updated_at` matches staging instead of trusting that somebody remembered. The lesson above is untouched; only the remedy moved |
| Checklist item 26 — correcting `legacy_id` to `legacy_bubble_id` in tier 2 — had already been done by a concurrent session, along with a whole new ADR 0007 that moved `HEAD` mid-build and answered an open question the checklist was about to answer differently | Noticed only because a README line referenced `docs/adr/0007-…`, a file that did not exist when the session started. Nothing else would have surfaced it before a merge conflict | Re-read `git log` and `git status` before the docs step of any build, not just at the start. **A plan written against a snapshot is stale the moment another session commits**, and the parts most likely to collide are exactly the shared documents every build touches — tier 2, the ADR index, the README |
| The reference-data seed was sized at 105 KB and declared comfortably under pre-commit's 500 KB limit. The artefact actually committed is a Python migration, where every dict key repeats on all 7,078 rows — it generated at **561 KB** and would have been rejected at commit time. The 105 KB figure was measured on a CSV rendering of the same rows, which is not the thing being committed | Only when the generator was run for real, several steps after the size question had been called settled and used to close an open decision | Rows are emitted as tuples with the column names stated once: 235 KB. **A measurement of something *like* the deliverable is not a measurement of the deliverable** — size, timing and row counts all have to come from the artefact that ships. The tempting fix was raising the hook's limit, which would have retired that gate for every file after this one; the same reasoning as `--strict` and `continue-on-error` above |
| A test asserting that `updated_at` moves was written to age a row backwards with a plain `UPDATE`, then compare before and after in one transaction. Both halves were wrong: the trigger overwrites `updated_at` unconditionally so the old value never lands, and `now()` is transaction-scoped so the two reads are identical regardless | Caught by reading the test back before running it, not by running it. It would have failed — and the failure would have looked like a broken trigger rather than a broken test | The trigger is disabled to plant the old timestamp, and the two writes are in separate transactions. **A test that fails can still be wrong**, and a red test is trusted about as uncritically as a green one — the instinct on failure is to fix the code it accuses. This is the second instance of the same root cause as the `set_updated_at` guard row above: reasoning about a sequence without checking what the database actually does with it |
| A markdown table row appended after a blank line renders as plain text, not as a row. It has now happened **three times** in the same two files: settled decisions 12–19, then two rows in this file, then — after both were fixed and the fix was written into the changelog — settled decisions 20–24 and rows 51–56 here. The same sitting also gave two rows the number 20, because a row appended to a fragment is not visually adjacent to the one before it | Never by a hook, and never by the person appending. Each time it was found by someone reading the rendered file for an unrelated reason, weeks of commits later. The raw diff looks perfect: the row is well-formed, the pipes align, and the only defect is a blank line that was already there | Append rows by anchoring the edit on the **last existing row**, not on the blank line or the paragraph after the table, so the new row cannot land across a break — and check the number against the row above it in the same edit. **Nothing in the gate reads markdown structure**: ruff, mypy, bandit and the layer check are all blind to it, so a broken table passes seventeen hooks. Fixing an instance does not prevent a recurrence, which is what the first two fixes assumed and why there was a third |
| Installing `fastapi[standard]` added **14 packages**, three levels deep among them `sentry-sdk` — an error-reporting SDK, in a repository whose whole vendor discipline is ports and an ADR per adoption. `[tool.check-layers.forbidden-external]` exists to catch exactly this and caught nothing, because it is a **denylist**: default-allow, so a vendor nobody listed is a vendor nobody guards. Being a transitive of an extra, it was never named in a dependency line either | Not by any gate. Every one of them passed — `pip-audit --strict` had no advisory to report, the layer check had no import to object to, and the lockfile diff is 258 lines nobody reads. It surfaced only because the dependency change was inspected line by line before being committed | `sentry_sdk` is denied in `domain`, `api` and `core`, and tier 2 gained a guardrail requiring an import name to join the denylist in the same change that adds the dependency. **A default-allow control cannot report what it does not know about, so its silence is not evidence** — the same shape as CodeQL rendering green while uploading nothing. Two limits are now written next to the entry rather than assumed away: it does not cover `main.py` or `api/deps.py`, which is where an `init()` would go, and it does nothing about a package merely shipping in the image. Still unguarded: nothing reviews what an extra pulls in, and `check_layers.py` itself has no test |
| `02_FIELD_MAPPING.md` maps legacy `Country of Origin` → `user_profiles.origin_country_code` and marks `OriginCountry(text)` **DROP — duplicate**. In the extract the coded field is empty on **all 19** profile rows and the text field carries every value; same inversion for `Country of study(mentor)` vs `StudyCountry(text)`. Following the canonical mapping literally would have migrated zero countries and reported success | Only by opening the JSON and counting fill rates per column before writing the transform. The mapping document is canonical under ADR 0007, it is specific, it names both fields, and it is confidently wrong — nothing about reading it suggests checking | Fill rates are counted per column from real data *before* a transform is written, not after it produces suspicious output. **A canonical document is canonical about intent, not about what the rows contain** — and the more precise it looks, the less likely anyone is to verify it. The package cannot be corrected in place (ADR 0007), so the correction lives in the model docstring and in the M1c handoff. Still manual: nothing compares the mapping against the extract |
| The cutover check for de-Bubbling images is specified as "every image URL query must return zero rows matching `%bubble%`". Avatars in the extract sit on **three** hosts — `app.edufurther.org` (11), `*.cdn.bubble.io` (5) and `lh3.googleusercontent.com` (4). The custom domain resolves to Bubble, so the check passes green while eleven avatars are still a live dependency on the platform being left. Five URLs are also protocol-relative (`//host/...`, no scheme) | Near-miss, found while inventorying avatar hosts for M1 rather than by running the check — which has never been run, because there is nothing to run it against yet | The check must enumerate hosts, not pattern-match a vendor name, and `app.edufurther.org` is on the list. **A substring check for a vendor's name stops working the moment the vendor is reached through your own domain** — and it fails safe-looking, because the query returns zero rows either way. Written down now, in M1, because the phase that would have discovered it is the one after files are already believed migrated |
| `docs/bubble-data-model.md` — canonical for the legacy shape, and the file tier 2 points at for exactly this — omits **five** `User` fields present in the extract: `betaUser(vb)`, `bookingCredit-WFCode`, `Slug`, `Qualification`, and a key literally named `null`. `Slug` is populated on 39 of 43 rows and is a public profile handle, i.e. a real product surface that no design document mentions | Only by enumerating keys programmatically across the extract and diffing against the document. Reading both would not have caught it: the document is a long hand-written list and the absence of an entry is invisible | `users.slug` is carried (decision #28) and the other four are dropped deliberately rather than by omission. **An inventory document is evidence of what somebody remembered, not of what exists** — and tier 2 already says Bubble field completeness cannot be fully automated, which is a statement about the Bubble editor being the authority, not licence to trust the transcription. Key sets are diffed against the extract per Thing before each phase's transform is designed |

### Worked example of a good row

| Failure | How it surfaced | Prevention |
|---|---|---|
| A security scanner had not run for six PRs | Job was green the whole time — the step failed on every run, `continue-on-error` swallowed the exit code | Scanners made blocking, every tool version-pinned, and a check that scans zero files now **fails** instead of passing. **A green job is not evidence a step ran.** |

That row is worth writing because the reusable lesson is not about the scanner.
It is that *green* meant "nothing objected," not "the check ran" — and that
distinction applies to every gate in the project.

## Rows worth stealing from other projects

These recur across codebases. Delete any that cannot happen here; keep the ones
that can, and let them earn their place before something teaches you the same
lesson at full price.

| Failure | How it surfaced | Prevention |
|---|---|---|
| A migration hard-coded existing constraint names, so the chain would not run against an empty database — no new environment could be provisioned | Only when CI first applied migrations from scratch; invisible for months | Guard every rename with an existence check in **both** directions; run the chain from empty in CI |
| A test asserted the buggy behaviour, so the suite stayed green through the whole incident | A person re-read the shipped behaviour weeks later | A green suite is not evidence of correct behaviour when the test came from the same misunderstanding as the code. Check the *requirement* against the test, not only the code against the test |
| Only the list endpoint was scoped; the action endpoint reached other owners' rows by id | Found during review of an unrelated change | Scope every read **and write** path through one shared predicate. A hidden row is not a protected row |
| A permissions bug that was really an identifier mismatch — a session's external id compared against a column holding an internal one | Debugged for hours in the permissions code, where the defect was not | Translate external ids once at the boundary; use distinct types so the mismatch is a type error |
| An adjacent line was edited instead of the intended one during a rename | Caught by the read-back step, before the next file | Name the line that must **not** change in the checklist item |
| A doc claimed a gate covered more than it did, retiring a manual check that was still needed | A defect the "covered" check could never have caught | State a gate's blind spots next to its coverage, every time |
| `persistence-patterns` requires a surrogate `id` on every table. It was overridden **twice** — decision #22 for ISO lookups keyed on their code, then M1 for 1:1 extensions keyed on `user_id` and a junction keyed on a composite pair — leaving **four different primary-key shapes across ten tables** one phase into a six-phase migration | Not by any gate. Both overrides were argued, written into tier-2 rows, reviewed and merged, and `ruff`, `mypy`, `check_layers`, `alembic check` and the full suite were green throughout. It surfaced only when the product owner read the models and asked why three tables had no id of their own | ADR 0015 makes the rule absolute, and a test walks the live schema rejecting a composite key, a key not named `id`, and an id the caller must supply — each proved by reintroducing it. **A convention enforced only by prose is re-decided by whoever reads it next, and each re-decision is locally defensible.** That is what makes this different from a mistake: nobody was careless, and the argument for each exception was good. The lesson is that "we wrote it down" is not enforcement, and a written rule with no check is a rule with a countdown on it |
| ADR 0009 §9 made `users.id` *be* the Supabase auth user id. Implementing it is what revealed the cost: every foreign key in all sixty-six tables would store a vendor-issued value, and the record contradicted a house convention that already existed — "three distinct identifier spaces, never interchangeable... translate at the boundary" — without citing or arguing against it | Not during the ADR's own review, which accepted it. It surfaced when the product owner asked the direct question: *does this mean every table referencing users is keyed on a Supabase id?* The answer was yes, and nothing in the record had said so | ADR 0014 reverses it — our own `id`, and `auth_id` as a nullable column. **An ADR that contradicts a settled convention should have to name it and argue against it**; 0009 §9 simply did not mention the identifier rule, so the conflict was invisible to everyone reviewing it. Still manual: nothing cross-checks a new ADR against the house conventions it touches. A side effect worth noting — the reversal removed the migration's hard dependency on calling the Supabase Admin API 1,200 times before its first `INSERT` |
| Three records — ADR 0007's Consequences, the tier-2 PII guardrail, and an M1b plan built on both — asserted that the Bubble extract contains **live OAuth credentials**, naming `calAccessToken` and `calRefreshToken`. They are expired **Cal.com** tokens from the integration the field mapping already marks DROP: Google's access tokens are opaque `ya29.` strings and never JWTs, and the accompanying numeric `calClientId`/`calDefaultScheduleId` are Cal.com's shape. Decoding `calAccessTokenExpiresAt` put both access tokens over 400 days past expiry and both refresh tokens over 50 | The product owner pushed back on a checklist that ranked redaction as the phase's headline security control, saying the field looked like Cal.com residue they had never used. Nobody had decoded a token in the four weeks the claim had been sitting in an accepted ADR, being cited forward | Records corrected in place; redaction kept as hygiene, since it costs a field list. **A doc can overstate a risk as easily as it can hide one, and the overstatement is the harder error to notice** — nobody audits a control for being too strong, so it silently reorders a plan around the wrong thing. The tell was that all three records described the credential by its *field name* and none by its contents: `calAccessTokenExpiresAt` was sitting in the same row, 13 digits of epoch milliseconds, and answered the question in one line of Python |
| The Bubble Data API and the Data-tab export use **different keys for the creation timestamp** — `Created Date` and `Creation Date`. The reader's docstring carried a table of the five ways the two sources differ, derived by reading both formats side by side, and this was not one of them | Dry-running the loader against the real 43-record export, which refused **every single record** for a missing timestamp. Loud — but only because something ran it against real data. Both unit suites were green, because every fixture had been written from the API sample | Both keys are canonicalised to `created_at`, a name **neither source uses**, so a transform cannot accidentally depend on one of them; a regression test asserts both readers agree. **Reading two formats side by side is how the first five differences were found and is exactly what missed the sixth** — a diff of two documents shows what is present in each, not what a consumer will ask for and find absent. The dry run existed for this and it worked; the lesson is to run it before believing a mapping, not after |
| Correcting that docstring table, the edit was applied to `infra/clients/bubble.py` — which has no such table. The `str.replace()` had no assertion, so it silently changed nothing, and the canonical table in `domain/bubble.py` kept saying "five things" beside six rows of evidence | The stale-reference sweep in the verification gate, grepping for the old key names. Nothing else would have: ruff, mypy, the layer check and 152 tests are all blind to a docstring, and the file that *should* have changed still read plausibly | Every scripted edit asserts its anchor is present before writing, which the same session had been doing elsewhere and dropped on this one. **A no-op replace and a successful replace are indistinguishable without the assertion** — and the failure mode is silent by construction, since the file is left exactly as it was |
| A test asserted that deduplicating a user's languages in the transform stops a duplicate hitting the composite unique index. It does not — `ON CONFLICT (user_id, language_id) DO NOTHING` absorbs it either way. The docstring named a mechanism that was not the one doing the work | Deleting the dedupe as part of a mutation batch and finding the test **still green**, while three other mutations in the same batch were caught. The prediction was that four would fail | The test now asserts what the dedupe genuinely protects: the *reported* count. Without it the loader announces two languages written where one row exists, and reconciliation is only worth having if its numbers are true. **A passing test under mutation is as informative as a failing one** — it says the code under test is not what the assertion depends on. Predicting which tests a mutation should break, and checking the prediction, is what turns mutation testing from reassurance into a measurement |

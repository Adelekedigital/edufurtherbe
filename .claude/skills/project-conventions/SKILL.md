---
name: project-conventions
description: This project's own settled decisions, house conventions, domain vocabulary, and guardrails — the things that are true here and nowhere else. Use at the start of any build in this repository, when choosing between two valid approaches, when a generic standard seems to conflict with how this codebase does it, before proposing a pattern the codebase has not used before, or when a requirement uses a term with a project-specific meaning.
---

# Project Conventions — EduFurther Backend

Tier 2 of the standards. The generic skills alongside this one are
project-agnostic and get overwritten on update; **this file is ours** and is
never overwritten.

## What this project is

Backend for a mentorship platform connecting African students pursuing
international graduate school with mentors at institutions worldwide. It is
replacing a Bubble low-code application, with a separate Next.js frontend, while
adding paid mentor sessions.

Two facts drive most decisions: **the data is small** (1,200 users, of whom 44
are mentors; 1,073 session bookings) and **the migration is a reshape, not a
copy**. Small data means correctness is affordable and throughput machinery is
not worth building. A reshape means the Bubble schema is an input to a mapping,
never a template.

Figures come from `docs/bubble-data-model.md`, which is canonical. Do not quote
the numbers on the public site — they are marketing figures and understate the
database by roughly 3x on mentors.

## Settled decisions

| # | Decision | Why | Reopen if |
|---|---|---|---|
| 1 | Python 3.14 only. No CI interpreter matrix | This is a deployed application, not a library consumed on many interpreters. A wider `requires-python` than CI exercises is an unverified compatibility claim | We ever ship this as a library, or a dependency blocks 3.14 |
| 2 | The package is `src/app/`, not `src/edufurther/` | FastAPI's own conventions use `app/`. The `src/` layer forces tests to import the *installed* package, so packaging mistakes fail in CI rather than at deploy | Never, absent a real import collision |
| 3 | `scripts/check.py` is the single definition of the local gate; `make check` is a thin wrapper | `make` is absent on Windows dev machines. Two hand-maintained copies of a gate drift, and the forgotten copy is the one that stops catching things | `make` becomes universally available here |
| 4 | ruff, mypy and bandit are pinned with `==` and kept equal to the `rev:` values in `.pre-commit-config.yaml` | They decide whether code passes, and their verdicts change between versions. A floor range let the venv drift a full major version from the hooks | Never. If it is painful, the fix is updating both sites together |
| 5 | Cut over behind a read-only freeze. No dual-run, no dual-write (ADR 0003) | A sync layer is the most defect-prone part of a migration, and here the schemas deliberately differ so it would have to maintain a bidirectional transform between shapes that never corresponded | Revenue or usage grows enough that a scheduled write outage is unacceptable |
| 6 | Bubble export is snapshot-first. The transport is the **Bubble Data API**, staged as raw `jsonb` in a `staging` schema, and still sits behind a port (ADR 0002, deferral resolved by ADR 0007) | The freeze is short and Bubble goes away after; a transform bug must be re-runnable against the original bytes rather than a rate-limited API. The Data API is subject to privacy rules, so per-Thing field-set verification is **mandatory** — a 200 response is not evidence of a complete record | Never for snapshot-first. The transport, only if the Data API proves lossy |
| 7 | Identity does not migrate. Accounts match on email; every user re-authenticates passwordlessly on first login — OAuth for Google or LinkedIn, and on the email path a **6-digit code by default with a sign-in link offered as a choice** (ADR 0009) | Bubble password hashes are not exportable, so re-authentication is a constraint rather than a preference. The mechanism is a code because a magic link fails predictably in this user base: Outlook Safe Links prefetches and consumes the token before the human clicks, and WhatsApp's in-app browser opens the link in a different session from the user's real one. Google was the dominant signup path, so for most of the 1,200 first login is one OAuth click and email is the minority case | Bubble ever exposes usable hashes |
| 8 | **Payments are out of scope for the initial build.** It delivers the core product backend and the Bubble migration; the legacy app has no payment integration to migrate | Nothing to carry across, and the pricing model is still being explored. Building a payment layer around an undecided model is how you get one you cannot change | The core backend and migration are done |
| 9 | A mentor sets their own **rate**; the platform offers a derived **pricing guide** as a suggestion | Mentors price their own time. The guide helps them choose without the platform setting the price | The platform ever takes pricing control |
| 10 | When payments arrive: money as integer minor units with an explicit currency; mentor payouts through an append-only **ledger**, paid by hand at first; the rate **snapshotted onto the purchase**, never read live from the profile | Floats do not represent money, and cross-currency is structural here. A ledger cannot be reconstructed after the fact. A live rate lookup means a mentor raising their price silently rewrites history | Never for the first two. The third only if rates become immutable |
| 11 | Payment provider: **undecided**, and not yet needed | Collections are African (Paystack/Flutterwave territory), payouts are global (Stripe/Wise territory). The two halves may not share a provider | — decide before payments work begins; it needs its own ADR |
| 12 | **Supabase** for Postgres, auth and storage, this phase. Used *as Postgres* — no PostgREST, no RLS as application logic (ADR 0005) | One vendor and one credential set during the cutover, and passwordless email login arrives without being built, which decision #7 needs for all 1,200 users. A policy in the database is invisible to `check_layers.py` and unreachable by a unit test, so authorization expressed there drifts from the Python that appears to implement it | Auth or storage needs to diverge from the database, or one shared failure domain across all three stops being acceptable |
| 13 | **FastAPI Cloud**, with Railway as the named fallback | Chosen deliberately. The escape stays real only by keeping a standalone Dockerfile exercised in CI and using no platform-native queue, cron or secrets store — an untested exit is an intention, not a plan | The platform cannot carry the freeze rehearsal, or a platform-native primitive becomes tempting |
| 14 | LLM access goes through a **provider-agnostic port**. No gateway | A port gives model swappability without a gateway's fee, extra hop, and flattening to the intersection of every provider. Adapters may use each provider's own caching, structured outputs and thinking configuration; the port must not degrade to the lowest common denominator | Never for the port. Which model is default is a config change, not a decision |
| 15 | Calendar is a **write target and an on-demand free/busy read** — never polled, never mirrored (ADR 0004) | Polling costs orders of magnitude more and is staler than reading at the moment of decision. Only mentors connect; mentees receive a calendar invitation and complete no OAuth flow | A proactive "your mentor's calendar now conflicts" feature is required, which on-demand reads cannot serve. Price the polling then |
| 16 | PWA push is **native Web Push** — VAPID keys, a service worker, no vendor | The browser does this. A notification platform earns its place at multi-channel fan-out with a preference centre, which is a later problem. iOS delivers push only to installed PWAs, so push is never the sole channel for anything that matters | A preference centre becomes a real requirement — then Novu, self-hosted |
| 17 | Institutions are the **hipolabs registry, mirrored** (ADR 0008, storage superseded by ADR 0020 — see #62). Surrogate `uuid` primary key, `domain` as the natural key, **no `ror_id`** | Bubble already runs on hipolabs, so this is the incumbent dependency rather than a new one taken on mid-migration. `education_entries.school_name_raw` is always kept, so an unmatched institution degrades display rather than losing data. *Populate-on-demand with live autocomplete was this row's original shape; it is unbuildable, because the API is `http://` only and a browser on an HTTPS page cannot call it* | Institution identity becomes a cross-system matching problem — mergers or renames breaking joins, or a partner exchanging institution records. Then ROR returns, and with it a backfill of every row collected in the meantime |
| 18 | WhatsApp is **platform→user transactional only**. Mentor↔mentee conversation stays in-platform (ADR 0006) | Handing both sides of a paid booking a direct off-platform channel is disintermediation by default rather than by decision | Never, while the platform takes a fee |
| 19 | ADRs use **Nygard** format, per ADR 0001 — *not* MADR | `/adr-new` ships with the tier-1 package and refers to the MADR template. Tier 1 is overwritten on update, so correcting the command would not survive; tier 2 does. Nygard is also the lower-ceremony format, and 0002 and 0003 already carry considered-options reasoning inside it without strain | ADR 0001 is superseded — which is itself an ADR |
| 20 | `docs/edufurther-migration/` is the **canonical target data model** — received, committed, and never edited here (ADR 0007) | It is the only artefact describing the target schema rather than the route to it, and part of its value is being a dated record from outside this repo. A transcribed copy would be a second copy that drifts from the DDL you actually run | A decision supersedes part of it — which is a new ADR, never an edit to the package |
| 21 | **Nothing ships earlier than the phase that creates a dependency on it.** In practice enums and lookups arrive with the phase that first uses them — with one standing exception: a foundation phase may carry a lookup whose rows the *next* phase's foreign keys require. `countries` and `languages` ship in M0 for exactly that reason; `institutions`, `degree_levels`, `service_offerings` and `scholarship_programs` wait for M2, and `legal_documents` for M1 | `00_foundation.sql` declares 40 enums and 7 lookup tables; exactly 2 of those enums are used by its own tables. Several encode business decisions this project has explicitly *not* made — `credit_source` contains `purchase` while payments are out of scope by decision #8 — and a shipped enum is a schema asserting a choice nobody took. ADR 0011 point 6 states the same rule in its shorter form, written before M0 was built; that record is accepted and immutable, so this row is where the exception lives | A phase genuinely needs a type before the table that constrains it |
| 22 | ~~**ISO lookup tables are keyed on their natural key**~~ — **REVERSED by ADR 0015.** `countries` and `languages` now carry a generated surrogate `id` like every other table; `code` and `code_639_3` remain `NOT NULL` and `UNIQUE`, and foreign keys store the id | The original argument still reads well and is preserved in ADR 0015: the ISO code is externally standardised and already the value a foreign key would store, so a surrogate means holding a UUID that must be joined back. It lost to a different consideration — **a rule with exceptions costs every reader the memory of the exceptions**, and two tables out of sixty-six behaving differently is what gets rediscovered and re-decided in M3. At this volume the join is free; the ambiguity is not | Never by argument. Only by an ADR superseding 0015 |
| 23 | **The `updated_at` trigger is attached per table, in the migration that creates it** | The handoff used a blanket function that scanned and re-triggered every table in the schema. That is non-deterministic across environments, invisible in a diff, and on Supabase `public` is not exclusively ours. Explicit costs one line per table and can be forgotten — which is why a test asserts every table carrying `updated_at` actually has the trigger | Never. If the per-table line is tiresome, the answer is the test, not a scanner |
| 24 | **Reference tables carry no `legacy_bubble_id`** | The guardrail applies to *migrated* tables. `countries` and `languages` are seeded from a published standard, not carried across from Bubble, and a column null on all 7,327 rows is not a migration anchor | A reference table ever does originate in Bubble |
| 25 | **Alembic is the migration chain; the package DDL is a specification, never executed** (ADR 0011). SQLAlchemy 2.0 + asyncpg; models are the source of truth for tables and columns, everything autogenerate cannot see is hand-written | Ten `.sql` files that run against an empty database describe a destination; a chain has to move a *populated* database. The moment production holds a row those files can never run again, so adopting them would mean a chain with one link. Models also make `alembic check` and the every-model timestamp test possible, and both are guardrails this project already claims | Never for the chain. Whether a given migration is autogenerated or hand-written is a per-migration judgement |
| 26 | **M1 creates exactly eight tables**: `users`, `user_profiles`, `auth_identities`, `user_onboarding`, `user_languages`, `admin_users`, `legal_documents`, `user_legal_consents`. Excluded and each recorded in the migration: `auth_codes` + `password_hash` (ADR 0009), and — under decision #21 — the three `phone_*` columns, `calendar_connections` (M3), `account_deletion_requests`, and the `about_me` FTS index (M2) | Four package documents name four different M1 table sets, differing over `calendar_connections`, `credit_lots`, `user_legal_consents` and `account_deletion_requests`. ADR 0011 makes the DDL authoritative over the prose, so `01_identity.sql` wins — but it is silent on calendar, whose DDL sits in the availability file and whose legacy `composioAuthId` values are known-dead. Somebody had to name the set; this is it | A later phase needs one of the deferred objects, which is when it ships |
| 27 | **`legacy_bubble_id` belongs on tables derived from their own Bubble Thing**, not on every migrated table. In M1 that is `users` and `user_profiles` only | The guardrail below says "every migrated table", and it over-states. `user_onboarding`, `user_legal_consents`, `admin_users`, `auth_identities` and `user_languages` are all derived from *columns of the `User` row*; they have no Bubble id of their own to anchor on, and their idempotency key is `user_id` — already unique, already the primary key on three of them. A nullable column that is null on every row is not a migration anchor | A child table turns out to have its own Bubble Thing after all |
| 28 | **`users.slug` is carried**, nullable, with a partial unique index and a `^[a-z0-9-]+$` CHECK | It is the legacy public profile handle, populated on 39 of 43 dev users, and it appears in **no** package document and **not** in `docs/bubble-data-model.md` — found only by reading the extract. It is exactly the globally-unique human-readable key D32 says to avoid while multi-tenancy is deferred, so carrying it is accepting that cost knowingly: the alternative is breaking every live profile link. The CHECK is deliberately looser than the shape all 39 dev slugs take, because 43 rows are not 1,200 | Multi-tenancy arrives — then slugs need namespacing, and this row is the record of what that will cost |
| 29 | **Bubble's `Creation Date` lands in `created_at` and its `Modified Date` in `updated_at`**, loaded with `trg_set_updated_at` **disabled**. No `legacy_created_at` / `legacy_modified_at`; `legacy_bubble_id` is the only legacy column | One anchor column instead of a parallel timestamp space, and `created_at` then means "when this account began" on every row rather than "when the ETL ran" on 1,200 of them. The hazard the withdrawn columns addressed is real and now sits in the loader: this trigger is unconditional, so a re-running idempotent importer would otherwise stamp every migrated row with its own clock — silently, with row counts and null-rate reconciliation both still passing. M1c reconciliation asserts `updated_at` matches staging rather than trusting that somebody remembered | Never for the shape. If the loader proves unable to hold the trigger off reliably, the answer is a reconciliation failure, not a new column |
| 30 | **Closed vocabularies live in `domain/enums.py`**, as `StrEnum` — unaffected by #100, which keeps the `StrEnum` and changes only what the database stores. ~~and `infra` imports them to build the PostgreSQL type~~ — **narrowed by #100:** `infra/db/types.py::pg_enum` remains how the **enum types not yet converted** (`PG_ENUM_TYPES`; the live count is in `docs/handoff-enum-to-text-check.md`) are declared, and a *new* vocabulary takes `text` + `CHECK` instead | These are facts about the product — what roles exist, which providers can link — not facts about PostgreSQL, and a domain service that needs `PrimaryRole` cannot import `infra`. Defining them beside the models would guarantee a move the first time a business rule mentions one. The helper exists because SQLAlchemy's `Enum` sends member *names* to the database by default, which silently creates types whose labels disagree with the package and with every hand-written query; centralising it means that cannot be applied to four enums and forgotten on the fifth | Never. Where a type name must differ from the class name, `PG_ENUM_TYPES` carries it |
| 31 | ~~**Those vocabularies are enforced as native PostgreSQL enum types, not as application-level validation.**~~ — **REVERSED by #100.** A closed set is `text` + `CHECK` in the database with a `StrEnum` at the Pydantic boundary. The enum types that have not converted yet stay until converted; `docs/handoff-enum-to-text-check.md` is the plan. The handoff's rule for *which vocabularies qualify* survives the reversal: if adding a value requires a code change anyway → closed set; if it does not → lookup table; if it is a standard → use the standard | Enforcing only in Pydantic would guard nothing during the phase we are in: **the ETL writes raw SQL**, so a transform bug could land `'Mentor'`, `'mentor '` or `'wizard'` in `primary_role` silently, and `psql` fixes bypass it too. Measured rather than assumed on PostgreSQL 17: `ALTER TYPE ... ADD VALUE` and `RENAME VALUE` both work **inside a transaction** on PG 12+, so the usual objection does not apply here. `text` + `CHECK` was the considered middle option — DB-level and trivially alterable — and loses because a CHECK cannot span tables, so M2–M6 would repeat and drift it per column | **This trigger fired, and that is why the row is struck.** It said: a vocabulary needs a value removed, `ALTER TYPE ... DROP VALUE` is *not implemented*, and that is the only expensive operation. `LEFT_EARLY` forced exactly that decision. The row correctly named the condition under which it would fail and was reversed on it — which is what a `Reopen if` column is for |
| 32 | **Every table has `id uuid PRIMARY KEY DEFAULT uuid_generate_v7()`. No exceptions** (ADR 0015). A 1:1 extension carries `UNIQUE (user_id)`; a junction carries a unique index on its natural pair; a demoted natural key keeps its own `UNIQUE`. **A future exception is an ADR superseding 0015, never a row here** | This rule already existed in tier-1 `persistence-patterns` and was overridden twice anyway — once for ISO lookups (#22) and once for 1:1 extensions and junctions — with every gate green through both. The overrides were argued, recorded, reviewed and merged; that is what makes the lesson general. **A convention enforced only by prose is re-decided by whoever reads it next**, and each re-decision is locally defensible. It is now enforced by a test that walks the live schema and rejects a composite key, a key not named `id`, and an id the caller must supply — each proved by reintroduction | Never |
| 33 | **A model module is named for the subject it owns, and models declare no `relationship()`.** `user.py`, `admin.py`, `legal.py`, `reference.py`; a new table joins the module whose subject it belongs to, or starts one | Without a rule, M2's `mentor_profiles` could defensibly go in `user.py`, a new `profiles.py`, or beside `user_profiles` — and five phases would answer differently, for 58 more tables. The absence of `relationship()` is currently doing real work and is invisible: it is why cross-module model imports stay acyclic, and under async a lazy-loaded relationship added later raises `MissingGreenlet` only after it ships. Loading is explicit at the query, per `persistence-patterns` | A genuine need for ORM-level traversal appears — then it is `selectinload`-style eager loading, declared deliberately, not a lazy default |
| 34 | **A module ships with its routers.** From M2 onward, a phase delivers schema, transform *and* the endpoints that read it — not six phases of migration followed by an API. The contract shapes are settled: RFC 9457 Problem Details, cursor pagination, normalisation at the boundary (ADR 0016) | Migrating everything first means the schema is never exercised by real usage until changing it is expensive. It also defers the two contract decisions past the point where they are free — tier 2 had already recorded that the error envelope and pagination must be settled before the *second* endpoint, and six phases of schema would have buried that | A phase genuinely has no read surface — then say so in the phase, rather than silently shipping none |
| 35 | **Every route carries a `summary`, a `description` and its documented failure responses; every tag carries a description of the group** | `/docs` is the contract the Next.js client is built against. A reader arriving cold should learn what a group is for and how a call fails without opening a route module — and the descriptions are the only part of the contract that survives someone reading the schema rather than the code. Nothing enforces it: a test walking the OpenAPI schema for missing summaries would, and does not exist yet | Never |
| 36 | **Normalisation happens at the boundary; the database constrains rather than transforms.** Pydantic normalises on the way in from the API, `domain/transform.py` on the way in from Bubble, and the column carries a `CHECK` asserting what both already guarantee | `users.email` was `citext` so comparison would be case-insensitive everywhere. Both writers already lowercase, so the type insured against a case that needs a *third* writer — and that writer is better served by a constraint that **refuses** than a type that silently accepts a second spelling nobody can find again. It also generated a Supabase advisory whose stated risk ADR 0005 had already removed. Declarative, on a shared schema mixin, so a new route cannot omit it by not thinking about it | A normalisation genuinely cannot be expressed at the boundary — then it is a domain rule, not a column type |
| 37 | **One canonical record, whichever source produced it.** `JsonExportSource` and `BubbleApiSource` both satisfy `domain.bubble.BubbleSource`; the transform never learns which it got. They differ on exactly six things and each adapter absorbs its own: the id field, whether email is flat or nested under `authentication`, array versus comma-joined list, `""` versus an absent key, timestamp format, and `Creation Date` versus `Created Date` | Every difference would otherwise be a branch in the mapping code, and a branch on provenance is a branch somebody forgets on one path. The sixth was found only by dry-running the loader against the real export, which refused all 43 records — reading two formats side by side shows what each *contains*, not what a consumer will look for and fail to find | A third source appears; it gets an adapter, not a branch |
| 38 | **Bubble timestamps go through one function, and it refuses to guess.** The API emits ISO-8601 `Z`; the export emits `Sep 3, 2025 5:31 am` with **no offset at all**, so `parse_timestamp` requires an explicit zone on that path and raises without one. The export renders in `America/New_York` — **measured, not assumed**: user `1701974206179x877854702892984200` reads `Dec 7, 2023 1:36 pm` in the export and `2023-12-07T18:36:46.179Z` from the API | A missing offset is absent data, not a parsing problem, and no cleverness recovers it. Defaulting to UTC is the one genuinely dangerous option: it would shift every migrated `created_at` by five hours — plausible-looking, wrong, and invisible to every row-count and null-rate check the runbook specifies | The Bubble app's timezone setting changes, which nothing here would notice |
| 39 | **`tzdata` is a runtime dependency, not an optional extra** | `zoneinfo` reads the operating system's database and **Windows has none** — `available_timezones()` returns zero, so every `ZoneInfo("Africa/Lagos")` raises on a developer machine while CI on ubuntu passes, and a slim container image usually fails the same way. `users.timezone` stores IANA names, a guardrail requires the mentor's zone as its own column, and M3 availability is entirely timezone conversion. Pinning the data makes the three environments agree instead of differing by platform | Never |
| 40 | **Auth accounts are provisioned eagerly, before cutover — not lazily at first login** (ADR 0018, closing the question ADR 0014 left open). Every mode of `scripts/provision_auth.py` is idempotent, and re-running is the recovery plan rather than a rollback | Eager is the only version you can prove works *before* the freeze: it runs end to end against a rehearsal database and reports a count. Lazy surfaces a linking defect one user at a time, after go-live, which is the worst possible moment to discover it. There is no undo for creating 1,200 auth accounts, so idempotence is not a convenience — it is the entire recovery story. A row already linked is skipped **without consulting Supabase at all**, so a re-run of 1,200 provisioned users costs zero API calls | Provisioning ever needs to happen for a population too large to run in one freeze window |
| 41 | **`--grant-admin` has no authorization check of its own, and that is the bootstrap rule.** The authority is **possession of the database credentials** — and only those. The mode never contacts Supabase and needs no service-role key. Every CLI grant lands in `admin_users` with `granted_by` null | There is no first admin to approve the first admin, so the chain has to start out of band. **The service-role key is deliberately not a second factor here** (ADR 0018, "The bootstrap grant"), and an earlier draft of this row claimed it was: anyone holding the DSN can `INSERT INTO admin_users` directly, so requiring the key in the CLI would document a control the system does not have — the exact failure this file logs for the `calAccessToken` claim. Null `granted_by` is the honest record of a grant made out of band — the same value every migrated grant carries, because the legacy option set recorded that someone was an admin and never who made them one. A synthetic "system" actor would look like knowledge we do not have, in the one table whose entire purpose is being auditable | An in-product admin-management surface exists — then grants made through it carry a real `granted_by`, and this stays the bootstrap path only |
| 42 | **When an adjacent vendor endpoint has an irreversible real-world side effect, name it in the adapter and let a test assert it is never called.** `infra/auth/admin.py` defines `INVITE_ENDPOINTS` and never uses it | Supabase has three ways to create a user and two of them send email. Reaching the wrong one does not raise, roll back or fail a health check — it delivers 1,200 emails to people who have not been told the platform moved, and there is no recall. A constant that exists only to be asserted *absent* converts "we meant to use the silent one" into a test that goes red on the mutation, which is the strongest guarantee available short of the vendor not offering the endpoint | Never — and the same shape applies to any future send, charge or delete |
| 43 | **One rule, one representation — and the copies that hurt are the ones no tool can see.** A predicate, mapping, constant or test double in a second place is a defect. Where a copy genuinely cannot be removed, the copies are pinned by a test that fails when they diverge, as `EXPORT_TIMEZONE` already is | Three of this project's settled decisions are *instances* of this — `pg_enum` exists "so it cannot be applied to four enums and forgotten on the fifth", `scripts/check.py` is "the single definition of the gate", one canonical Bubble record so a branch on provenance "is a branch somebody forgets on one path" — and **the rule above them was never written**, so each new case was decided from scratch and each decision was locally defensible. Then it cost twice in one unit: `deleted_at IS NULL` hand-typed into five statements with the `UPDATE` missed, and three private copies of one test double all carrying the same omission. Neither is visible to ruff, mypy, `check_layers` or coverage — a predicate inside SQL is not a symbol, and a fake is just more test code | Never. The *mechanism* is negotiable — extract, or pin with a test — the single representation is not |
| 44 | **`scripts/*.py` is a third composition root, and the gates do not cover it.** A script may construct concrete `infra` classes and wire them together. It may contain **no SQL and no business rule** | CLAUDE.md said `api/deps.py` and `main.py` were the only wiring points, and that was already false — `load_identity.py` builds an engine and a source. The real hazard is narrower and sharper: `scripts/check.py` runs `mypy src` and `bandit -r src`, and coverage measures `src/app`, so anything written under `scripts/` is checked by **ruff alone**. Provisioning put ~250 lines of async SQLAlchemy there, exercised by 650 lines of integration tests that counted for nothing against the coverage floor — the test file could have been deleted with the gate still green | The gate is extended to cover `scripts/`, at which point the SQL clause can relax and the composition-root clause still holds |
| 45 | **A result aggregate and its operator-facing rendering live in `domain/`**, beside the decision they summarise — `provisioning.Outcome`, not the store. `infra/etl/reconcile.Reconciliation` is the outlier and stays where it is | Two identical concerns in two layers is what a later author reads as "either is fine", and the one they pick is whichever file they saw last. `Outcome` is pure — counts and strings, no I/O — so the layer rule permits it in either place, which is precisely why prose has to choose. `domain/` wins because the counts *are* the decision's result, and a store that formats text for a terminal has stopped being a store | `Reconciliation` is next touched — it moves then, rather than being churned now for symmetry |
| 46 | **CORS is explicit origins, credentialed, and absent when unconfigured.** `CORS_ORIGINS` accepts a JSON array *or* a comma-separated list; empty or unset installs no middleware at all | The setting broke a deploy while wired to nothing: pydantic-settings JSON-decodes a complex type inside the settings source, before validators, so a bare origin failed as an opaque `SettingsError` at boot. Explicit lists are also forced rather than chosen — Starlette refuses `*` for origins, methods or headers once `allow_credentials=True`, and the frontend sends `Authorization` | Origins ever need to be wildcarded, which would mean dropping credentials |
| 47 | **Only environment keys generic enough to collide carry the `EDUFURTHER_` prefix** — `ENVIRONMENT` and `DEBUG`, and nothing else. `SUPABASE_URL`, `DATABASE_URL`, `CORS_ORIGINS` and the rest stand alone | A prefix on `SUPABASE_URL` buys nothing: it already names its vendor. On `DEBUG` it buys a great deal, because a host, a base image or another tool may define that word first. `DATABASE_URL` unprefixed is a small bonus — most managed platforms inject exactly that name. **The cost is real and is written into `config.py`:** the old blanket prefix let one validator prove any misspelling was a typo, and that now holds only for the two prefixed keys. A misspelled `SUPABSE_URL` is indistinguishable from an unrelated variable and leaves the default silently in place | A third key turns out to be generic enough to collide — it joins `PREFIXED_FIELDS`, and the guard follows automatically |
| 48 | **Verification depth scales with what a mistake costs** — the three tiers in CLAUDE.md. A checklist and approval come first at *every* tier | Everything was being verified as though it were the provisioning CLI: watch-fail, a twelve-way mutation batch, a stress run against real data, two review agents. That is right for 1,200 irreversible auth accounts and absurd for renaming an environment variable, and the uniform cost is a real part of why the early migration felt slow. The tier is stated in the checklist so it is agreed rather than assumed | Never — but a *specific* tier assignment is always arguable, and arguing it is cheap |
| 49 | **Two questions before writing code, beyond the generic rules in CLAUDE.md: where does the SQL live, and does this rule already exist somewhere?** SQL belongs in `infra/`, never `scripts/`. A rule that exists is imported, not retyped | Both were known and both were violated anyway, and each cost a whole extra pull request: SQL written into `scripts/` where no gate can see it, and `deleted_at IS NULL` retyped into a fifth statement that then missed it. Neither was a hard problem — they were questions nobody asked at the point where asking is free | Never |
| 50 | **A clarifying question offers 2–4 concrete options, a recommendation first, and one line on what each costs.** Plain words, no survey | CLAUDE.md rule 5 already says a genuine choice comes back for approval; it says nothing about *how*, and an unstructured question is one the reader has to do the analysis for. Naming the options and their consequences is what makes the answer a decision rather than a guess — and what lets it be given in one line | Never |
| 51 | **One fact, one home.** If it is in an ADR, the docstring is a pointer. If it is in a row here, the module does not restate the argument | The eager-provisioning rationale existed in an ADR, a settled-decision row and two docstrings at once — four copies of one argument, which is non-negotiable #8 applied to prose. Copies drift, and the reader cannot tell which is authoritative | Never |
| 52 | **Migrations are applied by the `Migrate` workflow, dispatched by hand — never on application startup, and never automatically on merge** (ADR 0017) | The serving platform has no release hook, but the deeper reason is independence: the workflow connects to Supabase directly, so changing host changes nothing about how a migration is applied. Dispatch-only because a schema change landing without anybody choosing the moment is how a lock queue meets a traffic peak; `production` additionally refuses any ref but `main` | A platform gains a release hook *and* the cutover no longer needs a rehearsable mechanism |
| 53 | **`service_offerings` is a closed six-row vocabulary — the *parents* of the legacy option set — and users cannot extend it.** No `status`, no `merged_into_id`, no `usage_count`, no `created_by`, and **no `parent_id` until children actually exist** | Package D12 says Bubble held two *separate* option sets with no mapping. It held **one**, used by both sides — but both columns store the display name as text at the moment of selection, so the mentee column carries six parents while the mentor column carries five parents plus five children and renames. Collapsing to the six parents is what makes a mentee's need and a mentor's offer the same row; the mixed-depth list is precisely why matching never worked, since "Document Review" could never meet "document preparation". Closed because this is the **matching axis**: free text turns "SOP help", "Statement of Purpose" and "sop" into three rows that match nothing, and the join simply returns fewer results with nothing to say why. The long tail lives where it belongs — `institutions` and `scholarship_programs` are both open, with the merge machinery to match | Children ship — they get the merge machinery and users may name an unlisted test or scholarship, **never a new top-level category**. Splitting or renaming a parent is the expensive move and has no `merged_into_id` behind it, so all future specificity goes into children |
| 54 | **A model module is named for its subject and splits at ~500 lines or 7 models** — `education.py`, `mentoring.py` and `scholarships.py` join `user.py`, `admin.py`, `legal.py` and `reference.py`. `mentoring.py` holds mentor *and* mentee tables, because `service_offerings` joins them and belongs to neither | Decision #33 names the rule and gives no threshold, so "is this module too big" stayed a fresh judgement every phase, for 58 remaining tables. `user.py` is 389 lines for 5 models, which sets the scale. The split is **by subject, never by size**: if `mentoring.py` outgrows the bound, mentor and mentee separate and `service_offerings` moves to its own module rather than being left in whichever half happens to be smaller | Never for the rule. The numbers are a tripwire for a conversation, not a limit to enforce mechanically |
| 55 | **`institutions.status` and `scholarship_programs.status` default to `'approved'`, so every user-facing write path must set `'pending_review'` explicitly** until admin curation ships | The package's default is fail-open: a write path that forgets publishes a user-created row globally and nothing objects. It is kept anyway, because flipping it before any write path exists would be a departure argued against code nobody has written. The obligation therefore sits on the writer — recorded here and in the migration docstring — and `test_status_defaults_to_approved` turns the eventual flip into a deliberate edit rather than a silent behaviour change | The pull request that ships admin curation: the default flips to `'pending_review'` there, and this row is the record that it was a known gap rather than an oversight |
| 56 | **Counts and ratios are derived at query time, never stored.** No cached totals, no denormalised counters, no percentage columns | Demonstrated twice and written down neither time. The package drops `countCompletedSession`, `countReviewReceived` and `percentageOfCompletedSession` from `Mentor (front search)` as "DERIVED at query time", and `mentor_profiles` explains why in its own comment: storing a derivable value "recreates the exact drift that made the front-search table untrustworthy". Then `usage_count` shipped anyway on two lookups — declared, indexed, explained as the admin queue's ranking signal, and **maintained by nothing**: no trigger, no function, no application rule, so it was zero on every row forever. Worse than useless, because the number exists to inform an approve-or-merge judgement and a stale one misleads exactly that. At 200–400 rows a `COUNT(*)` over a join is free | A count is genuinely too expensive to derive — which needs a measurement, not an intuition. Then it is a materialised view before it is a column, because a view states its own staleness |
| 57 | **`requires_booking_confirmation` defaults to `false`**, where the package has `true`, and the same value applies to migrated and new mentors | Legacy stored a blank on 10 of 15 mentors, and blank meant "never turned it on" — so `false` is what the data says. Making migrated and new mentors start on opposite settings would be a difference nobody could explain later. The exposure is bounded and that is what makes it safe rather than merely convenient: a mentor is bookable only when **approved** *and* **listed**, both of which they opted into, and in M3 a mentor with no availability rules has no bookable slots at all. `test_booking_confirmation_defaults_to_false` makes reverting it a deliberate edit. **The column left `mentor_profiles` under #88 and came back under #106; the default and its argument never moved, and the test is restored on the row that carries it** | Auto-confirm produces a real complaint from a real mentor. It is one column default and a migration away |
| 58 | **Legacy `meetingDuration` is an M4 input, not an M2 column.** It becomes the `duration_minutes` of the auto-created "General Mentorship" session type each migrated mentor receives | `02_profiles.sql` says mentor-level duration is "dropped" **fifteen lines below** the note that every existing mentor gets an auto-created session type in M4 — which is exactly where a mentor's chosen 15/30/45/60 belongs. The two lines were never connected, so following either one alone loses real data or invents a column the package refused. Duration is a property of the *offering*, not a global preference: a 15-minute question and a 60-minute document review have no meaningful shared default, ~~which is why venue and confirmation cascade and this does not~~ — **that half is amended by #88**: duration does not cascade from a *mentor-level column*, and never will, but an offering the mentor has not configured *was* to fall back to their **primary offering's** config. **Amended 2026-08-16 by D88's contract step: nothing falls back at all.** Every column on `session_type_booking_configs` is `NOT NULL`, so an unconfigured offering takes column defaults rather than another offering's values. The argument here survives it, because an offering with its own duration is never silently retimed | Never for the shape. If M4 ships without reading it from the staged snapshot, the data is gone and this row is the evidence it was known |
| 59 | **`user_scholarship_experience` and the `scholarship_relationship` enum never ship.** `user_awards.scholarship_program_id` carries the credential instead | The package pairs `user_awards` with a second table keyed on `awarded \| applied \| advised`. The legacy field behind it — `Mentor Services.Scholarship Experience` — has **no option set and no values on any row**, so there is no vocabulary and nothing to migrate; and it overlapped `user_awards`, giving "I won Chevening" two legal homes with nothing choosing between them. Dropping it left `scholarship_programs` with no consumer anywhere in the sixty-six-table package, which is why the nullable link was added: it restores one, and takes the `school_name_raw` + `institution_id` shape where the raw text is always kept and display never depends on the link. What is genuinely deferred is narrower than it looks — *applied* is a mentee goal and *advised* is a mentor capability, and both belong to the service hierarchy rather than here | A mentor needs to say "I advise on Chevening". That is the child level of `scholarships-financial-aid`, not a resurrected table |
| 60 | **A migrated row is attributed by the user-side link, never by `Creator`.** `Creator` is a cross-check: where it disagrees with the link, the link wins and the disagreement is *reported*. **Two** exceptions now: `Scholarship-Awards`, and — from M3 — `CalendarSettings`/`CalendarExtra`, neither of which has a user-side link in either direction, so `Creator` is the only path — and that is labelled an attribution guess where it is used | `plan_identity` established this and the reasoning generalises: the link exists in exactly one direction, so a row nobody points at cannot be attributed to anyone without inventing the attribution. `Creator` answers *who made the row*, which is not the same claim as *whose row it is* — an admin creating an award on a mentor's behalf is attributed to the admin, and no amount of care in the transform fixes that. It became an exact comparison rather than a fuzzy one when `Creator` was re-exported as a Bubble user id instead of an email, which also removed the whole normalisation surface and the ambiguity a duplicate address would have caused (`IdentityPlan.duplicate_emails` exists because that is a real possibility at 1,200 users) | A Thing gains a real owner column — then it joins the user-side rule and the exception shrinks by one |
| 61 | **Institution matching is a separate, re-runnable pass, not part of the load.** `education_entries` loads with `institution_id` null and `school_name_raw` always populated; linking is an `UPDATE` in its own phase | The link can then be retuned without reloading anything, which matters because the matching itself is delicate: measured against real school names, a genuine typo scores **0.773** and two different Nigerian federal universities score **0.750**, so no similarity threshold separates them. Exact and case-folded matches auto-link **unless the catalogue carries that name twice** — 73 names collide over 158 records and they cross borders, so `City University` (US, BD, GB) is reported as a question rather than linked to whichever row came back last; everything else is a suggestion for a human, and an unmatched entry still displays what the user typed (ADR 0008 point 5). Bundling it into the load would mean re-running a 940-row import to change a threshold | Never for the separation. The matching *rule* is expected to change, which is the point |
| 62 | **The institution catalogue is mirrored into `institutions` over HTTPS and refreshed weekly. The hipolabs HTTP API is never called** — not at request time, not in the ETL, not from a browser. `institutions.last_synced_at` records when a row was last seen upstream; `institutions.country_id` is nullable so a user-created row can be completed by an admin at review (ADR 0020) | The API is `http://` only, so a browser on an HTTPS page cannot call it — ADR 0008's client-side autocomplete is unbuildable, not merely deferred. The same data *is* served over HTTPS from the source repository, which is the asymmetry the decision rests on. Without a mirror a few hundred rows become the entire selectable universe, and every unlisted school becomes a `pending_review` row in a queue with no owner. Weekly is sized to the measured cadence — 100 upstream commits over 177 days, one change every ~2 days | If the match rate against the full 940-row import comes in materially below the measured 18/18, which reopens fuzzy matching. Or if hipolabs serves TLS, which makes a live path buildable again |
| 63 | **`status` filters *search*; it never filters an entity read.** Institution search and the scholarship-programme list exclude `pending_review` and merged rows; reading an education entry follows `institution_id` unconditionally | Otherwise the person who created a pending institution opens their own profile and the school is blank — the one user for whom that row is real, and the one waiting on the review. The two rules look like the same rule and are opposites: search decides what may be *offered* to everyone, an entity read renders what already exists | Never. If an entity read ever needs to hide a row, that is a deletion question, not a curation one |
| 64 | **Catalogue reads are unauthenticated and tagged `catalog`.** Institution search plus the five lookup lists. Non-catalogue public endpoints take the `public` tag; everything else takes its domain name | The data is public reference data — the world's universities, ISO countries and languages — and requiring a token breaks school selection during signup, before an account exists. Reading is public; **adding is not**: an unlisted school is created on the authenticated education write, in the same transaction, with `created_by` set to the caller, so nothing anonymous reaches `institutions` | A catalogue ever carries something not already public, or abuse makes rate limiting unavoidable — there is none anywhere in this codebase yet, and that is an accepted risk rather than an oversight |
| 65 | **A merge repoints `education_entries.institution_id`; `merged_into_id` is an audit trail, not a resolution step.** Search excludes merged rows, and the repoint runs in the merge's own transaction | Resolving `merged_into_id` at read time puts one rule into every query that touches institutions — profile reads, search, admin lists, exports, "who studied in the UK" — which is non-negotiable #8, and this project has already shipped `deleted_at IS NULL` retyped into five statements with the fifth missed. Merges also chain, so a read-time resolution needs recursion or a depth rule nothing enforces; repointing collapses the chain at merge time so depth is structurally zero. A wrong merge stays recoverable because `school_name_raw` is never overwritten | Never for the repoint. If merges ever need to be undone in bulk, that wants a record of what moved, not read-time resolution |
| 66 | **Writes are owner-only. A live admin reads these records and never writes them** — `OwnerDep` where the read uses `TargetUserDep`, one clause apart | An admin editing somebody's education silently is an audit trail nobody has designed, and opening it later is additive where closing it would be breaking. The two dependencies differ by a single parameter, so nothing but a test says which endpoint got which — there is one refusal test per write endpoint, plus one asserting an admin is refused while still permitted on the read | An admin surface arrives that genuinely needs to correct user data, and brings an audit record with it |
| 67 | **An institution a user types is created inside the education write's transaction, with `created_by` from the authenticated caller.** There is no endpoint that creates an institution on its own, and `CHECK (source <> 'manual' OR created_by IS NOT NULL)` enforces it | Search is public and read-only; creation rides on a request that already carries a token, so nothing anonymous ever reaches `institutions`. One transaction means a failure leaves neither row rather than an orphaned pending institution nothing points at. The review queue depends on the creator being known — a pending row with no creator is unanswerable | Never for the transaction. A separate create endpoint would need its own answer to both halves |
| 68 | **`degree_levels` is ISCED-aligned: Certificate/Diploma (4-5), Bachelor's (6), Master's (7), Doctorate (8).** Four levels, human display names, **no `isced_level` column** | The original six mixed levels with a specific qualification (`MBA`, which *is* a master's) and a career stage (`Postdoctoral`, which is a research post, not a degree — nothing is awarded). A filter could not answer "mentors with a doctorate". ISCED 2011 exists to make qualifications comparable across countries, which is this platform's whole problem. The column is omitted because nothing would read it, and `usage_count` was already deleted for being exactly that | A feature needs cross-country comparison numerically, which is when the column earns its place |
| 69 | **A 1:1 sub-resource gets a singular endpoint.** `mentee_goals.user_id` is unique, so it is `GET/PUT/DELETE /users/{id}/goal` — matching `/mentor-profile`, which carries the identical constraint | A plural endpoint over a 1:1 relationship tells every client that goals are a collection, and the client encodes it. Discovered by a write test hitting `uq_mentee_goals_user_id` on the second insert — the read had already shipped as a `Page` that could only ever hold zero or one. Going plural later is additive if the constraint is ever lifted; going singular later is breaking | The unique constraint is dropped, at which point the collection endpoint is a new URL rather than a changed one |
| 70 | **Admin grants are role-specific, and a caller without the right one gets 404.** `require_admin(*roles)` admits `super_admin` everywhere; `mentor_approval` decides applications and does not curate the catalogue; `limited_access` reads both queues and changes nothing | `AdminRole` already carried the distinction, so treating every grant as equivalent would have made the enum decorative — and this schema has deleted a decorative column before. 404 rather than 403 for the same reason as everywhere else: 403 confirms the endpoint exists and that somebody may use it. This is the one surface where the control is caller privilege rather than row scope, so each endpoint needs four tests — no token, no grant, **revoked** grant, live grant | A grant needs to be scoped to a subset of rows rather than to a kind of action, which is a different mechanism |
| 71 | **A mentor's approval and listing move together.** Approving sets both; declining unlists and records `never_approved`. An admin may decide their own application, and `approved_by`/`declined_by` record who did | Splitting them permits a mentor who is approved and invisible, with nothing to say which was intended. Self-approval is permitted because blocking it deadlocks a team with one admin — the control is that it is *visible*, not that it is prevented. `never_approved` rather than null because `ix_mentor_profiles_unlisted` is partial on `(unlisted_reason, unlisted_at)`, so a null reason makes the row unfindable by the query that index serves | An admin can unlist without declining — which is now true, and is why `mentor_status_events` is the next record rather than a fourth and fifth column |
| 72 | **`languages.is_common` marks CLDR's `modern` tier — 100 rows of 7,078 — and the other 6,977 stay.** The list is a literal in the migration, regenerated by `scripts/derive_common_languages.py`, with a test asserting the two agree | ISO 639-3 is a completeness registry, not a picker: searching "english" returned twenty creoles. Replacing the table with the common set was measured and rejected — it would delete Efik, Ibibio, Tiv, Kanuri, Idoma, Urhobo, Nupe, Gbagyi, Esan, Ebira and Jukun, every one a Nigerian language on a platform for African students, reversing the reasoning that chose 639-3 over 639-1. The migration holds a literal because a migration that fetches cannot be replayed and fails on a runner with no egress. `pcm` is excluded from the default **by our decision, not the standard's** — CLDR includes it — and stays searchable | CLDR's coverage tiers change materially, which the derivation script exists to make a one-command update |
| 73 | **`mentor_status_events` is the write path; `approval_status` and `listing_status` are a read model the database maintains.** Application code inserts an event and never updates those columns — `trg_apply_mentor_status` projects each event onto the one it concerns. Amends #71, which described both statuses moving in one `UPDATE` | Two columns per state hold only the *most recent* decision, so a mentor declined, re-applied, approved and then unlisted has lost three of four. The columns stay because `ix_mentor_profiles_searchable` indexes exactly those two and a constraint can be written over columns and not over "the state implied by the newest row in another table". A trigger rather than a helper everybody remembers to call: this repository has shipped that failure four times, starting with `deleted_at IS NULL` typed into five statements and missed on the fifth | A third dimension appears that the two columns cannot express — at which point it is another column and another trigger branch, not a second mechanism |
| 74 | **Each event states one fact and copies nothing forward.** `status_type` is a single enum — `approved`, `declined`, `listed`, `unlisted` — and approval and listing are written as *two* events, never one row carrying both | A row stating both dimensions must read the other one to copy it, so two concurrent transitions record a state that never existed. There is no `event_type` column beside `status_type`: the value already says which dimension it belongs to, and a second representation of one fact eventually disagrees with the first | Never for the shape. A new transition is a new enum value |
| 75 | **A mentor may undo their own pause and nothing else.** Self-pause writes `unlisted` with reason `mentor_paused`; resume is refused unless the **newest** unlisting carries that reason *and* the mentor is approved | Otherwise a suspension is a button the suspended person can press, and an admin unlisting somebody for review would be undone by the person under review. The approval half matters separately: a never-approved mentor relisting themselves puts an unapproved profile in the directory | An admin surface gains an explicit "allow self-relist" action, which is a decision rather than a side effect |
| 76 | **Every image entering storage is decoded and re-encoded by `domain/images.process`** — from the upload endpoint and from `scripts/migrate_assets.py`, which differ only in which source formats they accept | Copying bytes preserves EXIF, and a phone photo carries the coordinates it was taken at into a public profile. Re-encoding drops it by construction: there is no strip call to forget. It also means one population rather than two, so nothing later has to ask which images were cleaned | A format arrives that must be stored byte-exact — which would be a new requirement, not a shortcut |
| 77 | **Uploads go through the API, never a presigned URL** | A presigned PUT puts the client in direct contact with the bucket: nothing can strip metadata, nothing can resize, and the object name cannot be derived from the content. Those three are the entire reason the endpoint exists. The bytes passing through the API is the price, and the decode and the upload both run on a worker thread so one request does not stall the loop | Renditions move to a storage-side transform, at which point the bucket does the work and the endpoint hands off |
| 78 | **A replaced image is deleted, after the profile points at the new one, and never fatally** | Object paths are keyed on the user *and* a content hash, so no two profiles share an object and no other row can be pointing at the one being removed. Ordering matters: repointing first and deleting after means a storage failure leaves the old image intact, and a cleanup failure leaves an orphan rather than a broken profile. Uploading the same image twice lands on the same path and must delete **nothing** — the unconditional version deletes the image the profile now points at | Storage gains lifecycle rules that expire unreferenced objects, which does the same job without an application call |
| 79 | **A limit on the request body is middleware, and it counts the bytes rather than reading `Content-Length`** | Two findings, in that order. First, placement: FastAPI parses the multipart form to resolve an `UploadFile`, and Starlette's parser reads the stream to completion and spools it to disk *before* the dependency runs, so a check written there refuses a body already received — measured with an 8 MB body that arrived with `file.size` known. Second, mechanism: a header is a claim about a body that may not arrive in that shape. A chunked request carries no length, so nothing was compared and 7 MB went through in full against a real uvicorn server. The failure is worse away from the uploads than on them — a chunked `PATCH` is JSON, and `request.body()` accumulates JSON **in memory**. Both checks are kept: the declared one refuses before a byte is read, which counting cannot do; the count holds when the header is absent, which the declared one cannot. A *lying* header is not a third hole — uvicorn frames the body from `Content-Length` and delivers exactly what was declared, probed at 1,024 declared against 7 MB sent | Never for the placement or the count. The value moves when a route legitimately needs a larger body |
| 80 | **M3 creates exactly two tables**: `availability_rules` and `availability_exceptions`. **`calendar_connections` moves to M4**, amending #26's parenthetical that placed it here. Three departures from `03_availability.sql`, each deliberate: `availability_exceptions` gains `deleted_at`, its `legacy_bubble_id` is `{bubble_id}:{iso_date}` rather than a bare Bubble id, and `availability_rules` gains a partial `EXCLUDE USING gist` forbidding two windows overlapping on one weekday — the repository's **first** exclusion constraint, and the pattern M4's double-booking guardrail follows | #26 recorded calendar_connections as "(M3)" while deferring it out of M1 — a forecast of where it would land, not a decision that M3 needs it. Nothing in M3 reads or writes it: ADR 0004 puts the free/busy read at slot render and at confirmation, both M4, and ADR 0012 — which decides the OAuth arrangement its columns encode — is still **Proposed**. **Amended 2026-08-16**: the two behaviours ADR 0012 had not tested are now measured, and the answer put the calendar in EduFurther's own account rather than each mentor's — so a row records a mentor's `calendar.freebusy` grant alone, and **booking no longer creates a dependency on this table**. "Moves to M4" is again a forecast rather than a requirement. #21 governs, and its argument is the one that already removed `password_hash` and `auth_codes`. The `deleted_at` departure is the package contradicting its own "soft delete everywhere" rule rather than deciding anything. The anchor departure is forced by the data: one `CalendarExtra` row holds *discontiguous* dates — Jan 13, Jan 19, Jan 21, Jun 21, Jul 5 in the dev export — which cannot become one `daterange` without blocking the six months between them, so one legacy row fans out to one exception per date. The field mapping reads `block-Date(s) (list) -> date_range` and implies 1:1; it is 1:N. The overlap constraint is an invariant about data rather than workflow — true on every path, forever — so it belongs beside uniqueness in the schema and not in a write endpoint that the ETL and any future bulk editor would each have to re-implement. It is also only cheap while the tables are empty: adding one to a populated table means cleaning every violating row first. `'[)'` is load-bearing, letting 09:00-12:00 and 12:00-14:00 touch without colliding, and the partial `WHERE` keeps a switched-off or soft-deleted window from blocking the slot it used to occupy | ~~M4 ships calendar_connections with the free/busy read~~ — **that forecast did not hold either; see #82.** It ships with the direct Google adapter, whenever that is built. If a `CalendarExtra` row is ever found holding a genuine contiguous range, the fan-out needs a range-detection pass rather than one row per date |
| 81 | **`CalendarSettings` has two generations and only one of them can be read at face value.** A row carrying `timeZone` means the exported time IS the declared wall clock; a row without one was displayed to the mentor in **UTC**, five hours away. Generation A is **quarantined, never loaded** — reported with both candidate readings, decided once at the production extract. Availability is also the **second** exception to attribution-by-user-side-link after `Scholarship-Awards` (#60): `Creator` is the only path | The split is 12/12 in the dev export with no date overlap and a six-month gap — a rewrite, not a coincidence — and on every Gen A row the legacy `12hr-…-TXT` display column equals the UTC rendering of `startTime`. `parse_timestamp` returns aware UTC, so `.time()` on it is right for Gen A and wrong for Gen B, and the naive parse is the reverse: **one line, five hours, half the table, in either direction**. Quarantine rather than a guess because no dev mentor owns both an old rule and a booking, so nothing here can adjudicate it — and a wrong value written into a row a mentor may then edit can no longer be corrected by a re-run. `CalendarSettings` carries no owner column, `User` has no link to it, and `calendarSettingList` is empty on every row, which is what forces `Creator` | The production extract, where 1,073 bookings can test both readings and the tables are still empty. If bookings cannot separate them either, the quarantine stands and those mentors re-declare at cutover |
| 82 | **M4 creates exactly five tables**: `sessions`, `session_participants`, `session_events`, `session_types` and `session_type_booking_configs`. Deferred out of `04_sessions.sql`: `session_type_questions`, `session_type_question_options`, `intake_submissions`, `intake_answers` and `session_notes`. **`calendar_connections` is deferred again**, past #80's forecast that M4 would carry it. Five departures, each deliberate: `session_participants` and `session_type_booking_configs` take surrogate `id`s where the package uses a composite and an FK-as-primary-key; `session_events` carries **no `updated_at` and no trigger**; index names take `ix_` not `idx_`; and `sessions.session_type_id` ships nullable, with `NOT NULL` as a later contract migration | The intake stack is one unit — a submissions parent, its answers, the questions they answer and the options those offer — and half of it is worse than none. **All four landed together under #112**, so M4 now creates nine tables; `session_notes` is the one still deferred. None of the five has a legacy source or a read surface in this phase, so #21 governs exactly as it did for `password_hash`. The two surrogate keys are ADR 0015, which admits no exception and is now asserted against the live schema because prose lost twice. The `updated_at` departure resolves a contradiction rather than taking a position: the package gives `session_events` the column while calling the table "APPEND-ONLY. Revoke UPDATE/DELETE" fifteen lines above, and `mentor_status_events` already settled the shape. **`calendar_connections` is deferred for a new reason, not the old one**: the calendar integration goes direct to the Google API rather than through Composio, so `composio_auth_id` and the `calendar_provider` enum are the wrong columns to ship — see the ADR that amends 0012 | The intake tables ship with the booking write path that specifies them. `calendar_connections` ships with the direct Google adapter |
| 83 | **The package's field mapping is wrong on four points about `SessionBooking`/`SessionTracker`, each measured rather than argued.** (a) `Meeting venue` and `meetinglink` hold a **URL**, not a provider — so they feed `meeting_url` and `meeting_provider` is derived from the host. (b) `expiration` is `session time + 15 minutes`, a join-window cutoff, **not** a booking-request expiry, so it must not become an `expired` event. (c) `SessionDateTime-UTC` carries **no UTC shift** despite its name — `parse_timestamp(..., assume=EXPORT_TIMEZONE)` is correct, as for every other export field. (d) `Creator` is `'(App admin)'` on 105 of 105 bookings, so `created_by` is null on every migrated session | The package is canonical for the **target schema** (ADR 0007), never for what the legacy data contains — and on these four, following it produces wrong code with every gate green. (c) is the one that would have cost most and was nearly quarantined on the M4 handoff's advice: `Last Joined` is a *system clock*, so it cannot be shifted by application logic, and it sits within minutes of `session time` on 52/52 and 51/51 dev rows with **zero** in the 4–5 hour band. That comparison is between two fields of the same export, so the mentors all being in `America/New_York` does not weaken it. The merge itself is safe: across 103 linked pairs the two rows agree on mentor, duration, cancel flag, room and venue **103 times out of 103** | Re-run the `Last Joined` versus `session time` comparison at the production extract. If the two ever separate by roughly the export offset, (c) is wrong and every migrated `starts_at` moves |
| 84 | **Legacy *did* prevent double-booking, and mostly from the frontend.** A day's bookings were compared against the sessions table before a slot could be taken — described by the person who built it as rough, brain-racking manipulation forced by Bubble's constraints. So the absence of a database constraint was **not** the absence of a control, and `04_sessions.sql`'s heading "THE CONSTRAINT BUBBLE COULD NEVER ENFORCE" is a claim about the *database*, not about the product | This changes what the M4 pre-flight's result **means**, which is the only reason it needs recording. Frontend-side logic is advisory by construction: it cannot see a second person clicking in the same second, and it is skipped entirely by any path that does not go through that screen. So the honest expectation for overlapping live bookings in the production extract is **"probably none, but genuinely uncertain"** — not "none because it was enforced", and not "many because nothing checked". A non-zero result is a race or a bypass rather than dirty data, and is worth understanding rather than merely cleaning. It is also the sharpest available statement of what this migration buys: the control moves from the browser to the database, where it cannot be raced, bypassed or forgotten | Never — this is a fact about the legacy system, and the legacy system is going away. It stops mattering once the extract has run and the pre-flight has reported |
| 85 | **Double-booking is prevented by `sessions_no_mentor_double_booking`, an `EXCLUDE USING gist` over `mentor_id` and `session_window(starts_at, duration_minutes)`, partial on the live statuses.** `session_window` is a project-owned `IMMUTABLE STRICT` SQL function, and **the package's expression cannot be built** — `timestamptz + interval` is STABLE, so an index expression may not use it, and a `GENERATED ALWAYS` column fails identically. A stored `ends_at` column also works and is **rejected**: it is a derived value persisted (#56), and it drifts the moment `duration_minutes` is updated without it | The `IMMUTABLE` label is load-bearing and is a claim PostgreSQL trusts once and never re-checks — mislabelling it does not raise, it silently corrupts the index, and a corrupt index here means accepted double-bookings. It is earned rather than asserted: minutes are a fixed duration, measured identical across five zones over three DST boundaries, while the same expression with `interval '1 day'` differs by an hour. **So `session_window` must stay minutes-only.** Two tests hold it — one on `provolatile`, one on the behaviour — plus one that races two transactions for a single slot and asserts exactly one commits, which is the thing legacy's frontend check could never do. The discovery, and why executing DDL is the only way to check it, is in `failure-modes.md` rather than restated here | An hour or day component is ever genuinely needed — at which point the label is a lie and the mechanism has to change with it, not the function. Group sessions do **not** trigger this: they add a capacity rule and leave mentor exclusivity untouched |
| 86 | **A session is read by the people in it, and "my sessions" means either side.** `GET /users/{id}/sessions` lists every session the user is a party to, mentor or mentee, newest first; `?role=` is optional and additive. Authorization is `mentor_id = :viewer OR mentee_id = :viewer` in the `WHERE`, written once in `_is_a_party()` and reused by all three reads. **The admin asymmetry is deliberate:** an admin may list a user's sessions — `TargetUserDep` admits self or admin, as M3's profile reads already do — but `GET /sessions/{id}` and its events admit **parties only**, so an admin reaching one row by id gets a 404. Message bodies follow the people: the mentee writes `booking_message` and the mentor receives it, a cancellation's `reason_text` belongs to both, and an admin holding a session legitimately sees both | `?role=` was nearly **required**, on the belief that either-party could not use an index; measurement said otherwise (`failure-modes.md`). Requiring it would have meant a dual-role user could never see one list of their sessions, for a performance problem that did not exist. The list-versus-detail asymmetry is not an oversight to tidy: listing is oversight, reading a body is content, and the user's ruling that an admin may see message bodies is about *what* an admin sees, not *how they get there* — widening detail to admins is a later additive change, narrowing it back is not. `list_session_events` returns `None` rather than `[]` for a session that is not the viewer's, because `[]` claims "this session exists and has no history" and leaks existence to anyone who can enumerate ids (#70). **`meeting_url` goes to admins too, and that is a decision rather than an oversight** — raised at review because a meeting link is not information *about* a session but *access to* it, so an admin listing a user's history can join a live session unannounced, and ruled deliberately: leave it. Do not "fix" it by nulling the field for non-parties without asking | An admin console needs a session by id — add an explicit admin path with its own tests, do not loosen `_is_a_party()`. Group sessions arrive: `session_participants` becomes the party list and the two-column predicate stops being sufficient |
| 87 | **Bookable slots are public, computed on demand, and gated on `approved AND listed`.** `GET /users/{id}/availability/slots` takes no token — a mentee compares mentors before signing up. There is no viewer to scope to, so the mentor's own state is the control, and **both halves are required**: `apply_mentor_status` writes approval or listing and never both, and no CHECK ties them, so `pending` + `listed` is a legal row. Slots are never stored (package D18). The grid is anchored to the mentor's declared window and stepped by the offering's `duration_minutes`; a **booking removes what it covers and moves nothing**, while a **block re-grids**, because a block redefines the day and a booking only consumes it. Every session is subtracted **whatever its status** — a cancelled one keeps its slot until somebody deliberately releases it. The range is capped at 56 days and nothing before `now + min_notice_minutes` is offered. **`start` and `end` are optional and `session_type_id` is not**: `start` defaults to *the mentor's* today — never the caller's, which is unknowable without a token, and never UTC's, which silently skips a western mentor's current evening — and `end` to a week later. Defaulting and range validation both live in the store rather than at the edge, because the default needs the mentor's timezone and the range's legality depends on the default. It lives in `routes/slots.py` with `tags=["public"]` — **the first endpoint to use the tag #64 defined**, and its own module so the public/authenticated split is visible in the file list rather than in one decorator among ten | D20 says "render IF `listing_status = 'listed'`", which was copied into the first draft and would have published an unvetted mentor's calendar; D20 assumes an invariant this schema does not enforce. The all-status rule follows the user's ruling and M3's precedence argument — *a mentor with no booking beats a mentor double-booked*. The grid rule was written into the docstring, implemented as re-anchoring, and caught by an integration test disagreeing with the prose above the function: both readings step correctly by the duration, so the difference is invisible until something is booked, and then every other offered time silently moves. **A slot is not a reservation** — two callers see the same one and `sessions_no_mentor_double_booking` refuses the second, which is why nothing here checks-then-promises | A release mechanism exists for cancelled slots — that is a flag on the session set at cancellation, never an inference from `status` or from who cancelled. **`session_type_id` becomes optional once mentor onboarding auto-creates a default offering** — decided, not built; see #88 and #91. Today the M4 transform creates one "General Mentorship" type per mentor *who already had sessions* (five in the dev export), so a mentor with a profile and no booking history has none. ~~and there is no endpoint to create one~~ — **#109 adds `POST /me/session-types`**, so a mentor can now create their first offering; what is still not built is *onboarding auto-creating one*, which is what would make `session_type_id` omittable. ~~**Nor does anything expose session types at all**, so a public client cannot obtain an id to send~~ — **resolved by #92**: `GET /users/{id}/session-types` hands out the id, and an integration test walks the chain from discovery to slots. Until every mentor provably holds at least one, an omitted id has no safe fallback: "their only offering" breaks every caller the day a mentor adds a second, and a mentor may legitimately hold several — #58 settles that duration belongs to the offering, since a 15-minute question and a 60-minute review share no default. Group sessions arrive: capacity means a slot can be offered while already holding attendees, and the subtraction stops being all-or-nothing |
| 88 | **A mentor has a primary offering, and it is what unconfigured offerings fall back to.** `mentor_profiles.primary_session_type_id` — the offering a mentee lands on, which also drives display order. The fallback is one sentence covering every field: *an offering without its own X uses the primary offering's X*. That means `requires_booking_confirmation`, `default_meeting_venue` and `custom_meeting_url` **move off `mentor_profiles` into `session_type_booking_configs`**, so there is one fallback mechanism rather than two | A pointer on the mentor beats `is_default` on the config: a column holds one value, so "exactly one primary" is true by construction, the swap is a single atomic `UPDATE`, and `session_type_booking_configs` does not need `mentor_user_id` denormalised onto it merely to scope a partial unique index. Keeping the mentor-level columns *and* adding the primary-offering fallback would give an unconfigured offering two possible sources with nothing saying which wins — worse than either alone. This revises D21's cascade table and settled decision #57 | The column move is expand/contract on shipped tables and is **its own pull request**. The fallback itself is not needed until something can create an offering without a config — nothing can today, so it ships with mentor onboarding rather than before it. **Amended by #102**: `meeting_venue` leaves the fallback entirely — its terminus turned out to be reachable and empty. **Amended again by the contract step (2026-08-16): the fallback now has no members.** All three fields ended up per-offering and `NOT NULL`, or deleted; nothing inherits. `primary_session_type_id` survives and is not redundant — it still names the offering a mentee lands on and drives display order, which is what `trg_refuse_retiring_a_primary_offering` protects. It is simply no longer a *source* of values. **Dropped entirely 2026-08-17 by #107**, once that was the only claim left: nothing lands a mentee on it and nothing orders by it, because `_live_session_types` orders by `name`, which is unique per mentor among live rows and therefore total. **Amended again 2026-08-17 by #106: `requires_booking_confirmation` goes back to `mentor_profiles`, so this row's premise — one fallback mechanism covering three fields — now has no members and no fields. All three left it, each differently, which is what makes the premise rather than any placement the thing that failed** |
| 89 | **Availability gets a parent entity when per-offering schedules arrive: `availability_schedules`, with rules hanging off it and `session_types.schedule_id` naming one.** `is_default` belongs on the schedule, never on a rule | A schedule is five or ten rows; put the flag on each and four can say default while one does not, and moving a schedule between offerings means updating N rows. The single-row case is different — a booking config's parent already says what it is, which is why #88 uses a pointer there and this uses an entity. **`availability_rules_no_overlap` must widen to include the schedule** when this lands: it is scoped per mentor per weekday today, so "Working Hours" and "Evenings" both covering Monday would be refused | Only when a mentor genuinely needs different hours per offering. Until then one shared schedule is what exists and it is the right shape |
| 90 | **`is_active` means off, closed and hidden — one flag, deliberately not split.** Rows are removed with `deleted_at`, never a hard delete, and the front end states the impact before the act. A future recovery window (~60 days) and a purge job may follow | Splitting searchability from access is the right call for *mentors* (D20) and overkill for an offering: a switched-off offering should be invisible **and** unbookable, and a direct link to one already 404s. **`ON DELETE RESTRICT` does not fire on a soft delete** — it is an `UPDATE`, so the foreign key never sees it. ~~The primary offering is therefore protected by a **trigger** refusing `deleted_at`/`is_active` changes while `primary_session_type_id` points at it, following `trg_apply_mentor_status`.~~ — **struck by #107: the pointer is dropped and `trg_refuse_retiring_a_primary_offering` with it.** The `ON DELETE RESTRICT` reasoning above is still correct and still the reason a trigger was needed; what changed is that no column points at a session type any more, so there is nothing to protect. `tests/integration/test_offering_retirement.py` records the five deleted guard tests and what would bring each back. A purge job inherits the real keys: `sessions.session_type_id` is `RESTRICT`, so an offering that was ever booked can never be hard-deleted, and `booking_configs` is `CASCADE` | "Hidden but bookable by link" becomes a real requirement — then `is_active` becomes a status and this row is the record that the simpler shape was chosen knowingly |
| 91 | **Mentor onboarding creates the session type, asks for availability, and lets both be skipped.** The type is a template, not a finished offering. **Waitlist is a separate axis from readiness, not a value of it** | The mentor most needing a waitlist is the *popular* one — configured and fully booked — while a new mentor might collect interest before being set up at all. Both combinations exist, so folding them into one enum needs `not_ready`, `not_ready_waitlist`, `bookable`, `bookable_waitlist`; two independent fields cost nothing and the second is additive whenever it is wanted. Readiness itself needs no invented flag either: an offering carries its own `duration_minutes`, and its absence is the signal — a derived `bookable` boolean would be the same fact in two places (#8), and it reads as "has free slots", which is false for every fully-booked mentor | Waitlist is built. It needs a table, an interest record and a notify path, none of which exist, and it is explicitly not a priority |
| 92 | **`GET /users/{id}/session-types` is public, and the public visibility rule lives in `infra/db/public_visibility.py` — one copy, used by both public endpoints.** The path says *session-types* rather than *offerings* or *services*. The response is an allowlist: id, name, description, duration, notice and a **resolved** `meeting_venue`. ~~`category`, `application_stage`,~~ `created_by` and `custom_meeting_url` are absent — **the first two joined the allowlist under #111**, once each had a designed shape | `service_offerings` is already public at `/catalog/service-offerings` and is the axis matching joins on, so "offering" and "service" both already mean something else in this API — see the domain vocabulary. `category` and `application_stage` were free `text` with no CHECK, no vocabulary and no value in any row, so publishing them would have committed a public contract to an undesigned shape; removing a field later is breaking where adding one is not. **That reasoning is why they were withheld and why the bar for adding was high — #111 met it rather than overruled it**, and the argument still governs the next undesigned column. `custom_meeting_url` is a static room link, which is a bearer credential rather than a description, and a test asserts it appears nowhere in the body. The venue was `COALESCE`d onto `mentor_profiles.default_meeting_venue` because null on a config meant *inherit* (D21). **Amended 2026-08-16**: there is no COALESCE and no mentor column. `session_type_booking_configs.meeting_venue` is itself `NOT NULL` with a server default, so the value is still never null and the response field is still required — the guarantee moved from a cascade to the column. **Amended again 2026-08-17 by #108**: the `COALESCE` is back and the word *resolved* is literal again — the offering's own conferencing option, then the mentor's default, then the platform fallback. The guarantee moves from a column to that last step, which is why it is three steps and not two. `custom_meeting_url` is gone too, so the test asserting it never appears in the body went with it; the **allowlist** assertion is what still catches a field of any name being added to this payload | #88's column move changes where the fallback comes from — this endpoint keeps resolving, only the source moves. The predicates in `public_visibility.py` are a **control**, not a helper: a third public endpoint reuses them, and a copy is a defect rather than a style choice |
| 93 | **A session names the people in it, and the party lookup carries no soft-delete predicate.** `SessionRead` embeds a `mentor` and a `mentee` — `first_name`, `last_name`, `avatar_url` — beside the ids, which stay. `users` is joined **inner** and `user_profiles` **outer** | The authorization on a session read is the session itself, not either party's lifecycle: a mentee had a real session with a real person, and their own history must not decay into a UUID because that person later left the platform. That is deliberately the **opposite** of #87 and #92, where a deleted or unlisted mentor disappears, and the difference is which question the endpoint answers — *may the public see this mentor* versus *what happened to me*. The join asymmetry is the same reasoning applied to reachability: `sessions.mentor_id` is `NOT NULL` with a foreign key so the user row is guaranteed and an outer join would be a guard no mutation could reach, while nothing guarantees a `user_profiles` row and an inner join there would drop the session from **both** parties' lists. Names are nullable because the columns are — the M2 transform maps them from optional Bubble fields — and the nulls go out unchanged rather than becoming a server-side placeholder in one language that no client can override | Account deletion is built and needs names scrubbed from other people's history — that is a tombstone on this lookup, one predicate in one place, and the mutation asserting the predicate's *absence* is what will fail first and point at this row |
| 94 | **The public mentor profile is `GET /mentors/{handle}`, gated on `mentor_is_public()` alone, reachable by id *or* legacy slug, with session types inlined.** D20's three-clause rule — listed, *or* the viewer has a session, *or* admin — **becomes one clause here** | The middle clause is dropped by product decision: a mentee with a session sees *that session*, which carries the mentor's name since #93, so nothing breaks when a mentor pauses. The admin clause is dropped because admins read the owner-facing endpoint, which names whose records are being reviewed. Together they remove the need for an **optional-token** dependency — a shape this codebase has nowhere else, which would make every response vary by caller and could be downgraded to the anonymous path by corrupting one byte of a token. The handle is *parsed* rather than `OR`-ed: `id = :h OR slug = :h` asks the planner to combine a primary key with a partial unique index for a value that can only match one, and `_by_handle()` returns a predicate rather than a query so there is still exactly one statement and one spread of the guard. Inlining is safe **only because** it calls `list_session_types()` — a second query returning roughly the same shape is how two endpoints start disagreeing, and a test asserts the inlined list equals the standalone one | A slug write path arrives — it must refuse UUID-shaped slugs, since `^[a-z0-9-]+$` permits one and the parse would make it unreachable. Mentor discovery arrives and wants `GET /mentors`, which this prefix already leaves room for |
| 95 | **`GET /mentors` lists mentors who are *bookable*, never those who are *free*, newest first by `mentor_profiles.id`, with no filters.** Bookable is `mentor_is_public()` plus an active offering that has a duration and an active availability rule. A mentor who has not finished setting up does not appear at all; their profile still resolves by direct link | Availability cannot be a `WHERE` clause — it is a computation over projected windows minus bookings — so filtering on it means computing slots for every candidate *before* paging, which stops the cursor being a database keyset. That is a different pagination model, not a slow query. Caching it is the drift D20 rejected, where a stored `is_available` was wrong the moment somebody booked. The two halves of bookable fail differently and both matter: no config means `/slots` **404s**, and a card linking to a 404 is worse than no card; no availability rule means `/slots` returns **200 empty**, and rules are recurring, so none means no slot will ever exist until the mentor acts. Service offerings are deliberately *not* required — a mentor who claimed none is bookable and simply matches no filter. Sorting on `mentor_profiles.id` rather than `users.id` orders *became a mentor* rather than *signed up*, and the cursor is therefore a value the row does not contain. Filters are deferred under #21: a query parameter is additive, a sort order and a row shape are not. **`?q=` searches** across a weighted seven-field document — name at `A`, headline and programme at `B`, schools and both countries at `C`, bio at `D` — with **two configurations chosen per field**: `simple` for the four name-shaped fields, because `english` reduces *Harding* to `hard`, and `english` for the three prose fields, which is what M2 already chose for `about_me`. The query is parsed both ways and OR'd. **Search pages by offset while browse keeps its keyset**, both behind the one opaque token: a rank is not in the row and not stable, and "best match" will grow to include quality signals that do not exist yet, so a token encoding today's formula is invalidated the day it changes. Capped at 500 deep, which is what search products do rather than solve. The document is computed inline and is therefore a sequential scan by construction — ~275ms at 500 mentors and ~3.9s at 5,000, crossing D19's 200ms line somewhere between **500 and 2,000** — a range, not a point, because the same unchanged statement measures 50ms or 285ms at 500 depending on machine load, so figures from different sessions cannot be compared and only back-to-back A/B deltas mean anything | The frontend knows which filters it wants — service, school, degree and country are all reachable today and four of their indexes already exist, three arriving as `EXISTS` because `education_entries` is one-to-many. Program needs a vocabulary first: it was a Bubble **option set** flattened to text at migration, and D14 records where that leads. Ordering becomes a fairness lever once the list outgrows one page — changing it changes the cursor |
| 96 | **The token minter ships in `src/`, and that is not a hole.** `infra/auth/dev_tokens.mint_dev_token` builds the claim set once; `tests/conftest.api_token` and `scripts/dev_token.py` both call it. The script runs against whatever `DATABASE_URL` is set, names the host it used, and refuses only when the token could not work: a configured `SUPABASE_JWKS_URL`, or no symmetric secret | The claim set is a rule, and it existed in `conftest` before a developer needed the same token by hand — so the alternative was a second copy, which is #43 in the form that already cost this project twice. Shipping it in `src/` looks like a forgery tool and is not: `SupabaseTokenVerifier` **prefers JWKS whenever one is configured**, so a real project rejects anything signed here, and against a deployment holding an HS256 secret the function grants nothing that holding the secret did not already grant — signing a JWT is five lines of `pyjwt`. That is the same reasoning as #41: a control the system does not have should not be documented as though it does. The refusals exist because the failure they prevent is unreadable — a token minted against a JWKS deployment comes back as a bare 401, debugged in the application rather than in the environment. **A local-only host check was built first and removed deliberately**: the signing scheme already decides this, so the check blocked a legitimate lookup against a non-local database while guarding nothing, and the host is reported instead | Supabase's symmetric secret stops being accepted at all, which removes the mechanism rather than the decision. **Do not "fix" this by moving the minter into `tests/`** — `scripts/` cannot import from there, so the copy comes straight back |
| 97 | **Text arriving from a client is made storable at the boundary, and what cannot be stored is removed rather than refused.** One function, `storable()` in `api/schemas/common.py`, reached two ways: `Normalised` calls it for every request body, and the `StorableText` annotation carries it onto the three `q` query parameters, which reach no model. **Numbered 97, not 96** — PR #74 claims 96 and is unmerged | A `str` Python accepts is not the set a PostgreSQL `text` column accepts, and the gap has **two halves that fail in different layers**. `U+0000` encodes to UTF-8 fine and the *server* refuses it (`CharacterNotInRepertoireError`); an unpaired surrogate never reaches the server at all, because UTF-8 has no encoding for one and *asyncpg* raises `UnicodeEncodeError` building the message. Both are **a 500 with a stack trace from one anonymous request, on endpoints that take no token**, and no gate sees either: the annotation is `str`, the value is bound rather than interpolated, the SQL is correct, and both are legal Python. **The rule is therefore not a list of characters.** The first version of this row said `U+0000` was the only one; that claim was probed rather than believed and it was false, so the definition is now the encoder's — encode with the database's encoding, drop what it cannot represent, then drop the one thing it can represent and the server still rejects. Everything else survives: C0 controls, non-characters, astral-plane emoji, all probed. Removed rather than refused because `Normalised` **already rewrites what it is given** — trims, and turns `""` into `None` — and a base class that quietly repairs whitespace while hard-failing a control byte is two rules wearing one name. Declared as a **type** rather than called per dependency, because all three parameters that existed were reachable and the fourth is the one that would ship | The surrogate half is reachable through exactly one field today — `AvailabilityExceptionWrite.reason`, the only free-text field with no `max_length`, because a length constraint makes pydantic-core parse the value as unicode and refuse it with a 422 first. **That safety is somebody else's behaviour**, pinned by a test: if it changes, every free-text field becomes a route. A field that must round-trip exactly — a code block, a paste buffer — is when removal stops being free; nothing stores one today |
| 98 | **The mentor card's academic line is chosen at query time, and `completed_sessions` means both parties turned up.** The entry shown is the highest `degree_levels.sort_order`, then the latest `date_end`, then the id — never `education_entries.is_most_recent`. The degree renders `COALESCE(education_entries.degree_abbreviation, degree_levels.short_name)`, the programme is **`study_course`** and not `study_program`, and a user with no education degrades to nulls rather than vanishing | **The export contradicted the schema twice, and both would have shipped past a green suite.** `is_most_recent` is blank on 21 of 21 rows: legacy owned the flag, had write paths for it and never set it, which is what a value derived from *other rows* does — it is a property of the set, so any sibling added, edited or deleted invalidates it (#56, and the `usage_count` column this schema already deleted for being zero forever). And `study_program` holds degree *names* like `BSc (Bachelor of Science)` on 8 rows, while `study_course` holds `Mathematics` on 21. Level outranks recency because the card is a credibility signal and a second bachelor's must not hide a doctorate. `completed` excludes `no_show` because a session the mentee missed held the mentor's time and delivered nothing — the nuance lives on `session_participants`, which is what an attendance figure reads. Derived never stored: the package lists `countCompletedSession` on *Mentor (front search)* and drops it as "DERIVED at query time" | `is_most_recent` is **unusable and unread**, and removing it is a two-release change of its own (`db-migration`) rather than a line in a feature PR — the column is mapped, so dropping it in the release that stops referencing it breaks any instance still running the previous code. Mentor stats — mentoring time, mentees mentored, average attendance — need their own definitions before they are built, and one of them is not the sum of scheduled durations |
| 99 | **A blocking defect found mid-build is fixed in that build, and a test whose result depends on *when* it runs is a defect.** If the gate is red for a reason outside the current scope, the choice is fix it here or stop — not merge around it. Time-dependent tests are named specifically because they are the recurring instance: inject the clock, as `list_slots(now=...)` does, or assert against a value the code produced rather than against `now()` | Deferring a red gate hands the next person a branch that cannot be verified, and the cost compounds — every subsequent PR either inherits the failure or learns to ignore it, and a suite people ignore stops being a gate at all. The two instances so far were both invisible until a date passed: `assert n == 28` failed five hours in every twenty-four (#71), and `SYNCED + 7 days > now()` passed for exactly seven days and then never again. **Both were written green and were already wrong.** A calendar is not a fixture | A test genuinely needs real elapsed time — none does today, and the two that look like it inject a clock instead |
| 100 | **A closed set is `text` + `CHECK` in the database and a `StrEnum` at the Pydantic boundary — not a PostgreSQL enum.** And for the enum types that have not converted yet: **do not add a value until something writes it** | PostgreSQL cannot remove an enum value — `ALTER TYPE ... DROP VALUE` is *"not implemented"*, verified against this project's own database. So `ADD VALUE` is one line and every speculative label is permanent. Eight already are: `left_early`, `zoom`, `override`, `linkedin`, `observer`, and three of four `verification_status` members, each documented in the codebase as having no producer. A `CHECK` gives the same guarantee and is freely altered. **The guarantee has to stay in the database**, not move to Pydantic alone: the ETL and `scripts/load_*.py` write these columns with hand-written SQL and never construct a model, so API-only enforcement leaves the path that has produced every data surprise this milestone unguarded. `institutions.source` is the existing precedent. ~~and `mentor_status_events.reason` already runs the whole pattern by accident — Python enum, `text` column, `.value` written~~ — **corrected 2026-08-16:** `reason` is a `text` column carrying a Python enum, but it is **not** the pattern and takes no `CHECK`. It holds free text on a decline and free admin text on an unlisting (`DeclineRequest.reason` reaches it via `set_listing`), with `UnlistedReason` values as *sentinels* read back by equality. `UnlistedReason` therefore sits in `UNCONSTRAINED_ENUMS`, not `TEXT_CHECK_ENUMS` | Converting the remaining columns (21 at the start; `docs/handoff-enum-to-text-check.md` tracks what is left) (22 before D88's contract step): scoped and audited in `docs/handoff-enum-to-text-check.md`, including the seven indexes with enum predicates, the gist exclusion constraint that guards double-booking, and `trg_apply_mentor_status`, which reads one enum column and writes two more — a `DROP TYPE` under it succeeds silently and fails at the next write |
| 101 | **A mentor's stats are derived per request, and `completed` means both parties turned up.** `session_stats.delivered()` is the single predicate the discovery card and the profile both read. Attendance is the mentor's own — attended over expected — with `pending` and a missing participant row both counting as *unknown* rather than absence, a denominator of terminal sessions only, and `null` rather than `0` when nothing is known | D56 already says counts and ratios are never stored, and the migration package drops `countCompletedSession` from this exact card as "DERIVED at query time". **The `completed` ⟺ attended invariant is not merely unenforced — the migrated data already violates it**: 3 of 36 loaded `completed` sessions have an absent party. The 103 linked booking-tracker pairs do agree, but most sessions come from the 164 *orphan* trackers, whose status is synthesised as `CANCELLED if cancelled else COMPLETED` and never consults attendance. A `CHECK` cannot tie the two columns because they are in different tables, so each stat answers from the column it is about and a contradiction in the data shows as a contradiction in the numbers rather than being hidden by one of them. Zero percent says "never shows up" where null says "no data yet", and until the lifecycle that marks attendance exists, unmarked is the state of every session booked after cutover. `mentoring_minutes` is scheduled duration because Daily can supply measured time and Google Meet cannot, and one number with two definitions by venue is worse than one honest one | The session lifecycle is built — it must derive status **from** attendance so there is one place it is decided, and a data-integrity check belongs in the ETL reconcile step. Daily's per-participant `duration` makes measured minutes reachable for that venue, which is a *second* stat rather than a redefinition of this one |
| 102 | ~~**`session_type_booking_configs.meeting_venue` is `NOT NULL` with a server default, so the venue half of D88's fallback does not exist.** Every offering carries its own venue; nothing inherits one.~~ — **the column is dropped by #108 and venue inherits again, from the mentor's default rather than from another offering.** ~~The mentor-level `requires_booking_confirmation` toggle **fans out to all of that mentor's live offerings** rather than to the primary alone~~ — **struck by #106: the fan-out is deleted and the toggle writes one mentor row** | D88's fallback sentence and D92's required `meeting_venue` field contradict each other, and neither record knew it. The cascade's terminus was `mentor_profiles.default_meeting_venue`, which the contract step drops — and `trg_refuse_retiring_a_primary_offering` makes *release the pointer, then retire* the sanctioned two-step, so **live offerings with no primary is a state the guard creates by design**, not an edge case. Resolving through it yields the null a required field forbids. Of the three exits — optional field (breaking a shipped contract), forbidding the release (making a primary swap impossible), or per-offering venues — only the third costs nothing already shipped, and the expand step's backfill had already written a real venue onto every row. The fan-out reproduces today's behaviour exactly: one setting for one mentor, now stored per offering | Until an offering-management endpoint exists. That is what per-offering control needs, and the fan-out is what has to go when it lands. **Corrected 2026-08-16 by the contract step**: this said `requires_booking_confirmation` keeps the fallback and only `meeting_venue` leaves it. That was wrong when written — the config column is `NOT NULL` and its own model says *"not nullable and not an inherit"*, for the same reason stated differently: a boolean has no room for a third state. **After the contract step D88's fallback has no members.** Every column on `session_type_booking_configs` is `NOT NULL`, `custom_meeting_url` was deleted rather than moved, and nothing inherits from a primary offering. See the amendment on D88 for what the pointer is still for. **Reversed in part 2026-08-17 by #106**: the config column is nullable again and null means *inherit from the mentor*, which is not the state this row rejected — that argument was about inheriting from a **primary offering**, whose terminus can be missing. The fan-out is deleted with it. **And #107 removes what remained of the cascade's machinery** — `primary_session_type_id` and its guard — so "live offerings with no primary" is no longer a state the schema can be in, because there is no primary |
| 103 | **A mentor's own offerings are `GET /api/v1/me/session-types`, and the owner-facing predicate is a *narrower* one rather than a flag on the public predicate.** `session_type_of()` is ownership + soft deletion; `session_type_is_live()` is that plus `is_active`. The owner read consults neither `mentor_is_public()` nor `is_active`, ~~returns `category` and `application_stage` that the public contract withholds~~ — **both are public since #111**, so the owner view's only remaining addition is `is_active`, and answers a non-mentor with **200 and an empty page**. Deleted offerings stay absent | **The `/me/` path is a departure from M2's precedent and it is forced, not preferred.** M2 addressed sub-resources as `/users/{id}/…` "because a platform admin needs one user's education" — but `/users/{user_id}/session-types` is already a **public, tokenless** endpoint, so serving an owner view there needs an optional-token dependency, which #94 refuses outright as a shape this codebase has nowhere else and one that a corrupted byte downgrades to the anonymous path. The **consequence is real and accepted**: no admin can see a mentor's inactive offerings, where under the M2 shape they could. Widening later is additive. The predicate is composed rather than given `include_inactive` because `session_type_is_live()` decides what is **bookable** and is spread by `slot_store` and `profile_writer` too — a mis-defaulted flag reaching either makes a switched-off offering bookable again, against #90, silently and one keyword away; a predicate taking no argument cannot carry that mistake. An empty page rather than 403 follows the public endpoint's own reasoning: `session_types.mentor_user_id` references `mentor_profiles`, so a non-mentor **cannot** own a row and "you have none" is true, where 403 answers a question nobody asked. The two fields were withheld publicly to avoid committing a public contract to an undesigned shape — neither reason applied to the person who typed them, and **neither applies at all since #111 gave both a designed shape** | **#109 adds `POST`/`PATCH`** and the delete follows, at which point a `session-types` tag may earn its place over `users` (a tag is documentation grouping, not a wire contract, so regrouping breaks nothing) and the **inner join to `session_type_booking_configs` has to be re-decided** — it matches the public endpoint today and excludes no row the product can reach, because nothing in `api/` can yet create an offering without a config. A `LEFT JOIN` would make three required response fields nullable, which is breaking, so it belongs to the release that can actually produce such a row. An admin path is additive whenever one is genuinely needed |
| 104 | **Booking notice is a platform rule of 24 hours — no same-day booking — and it lives in `session_type_booking_configs.min_notice_minutes`' server default.** Mentors raise it per session type, 24h–72h, enforced at the Pydantic boundary. The column's `CHECK` is **sanity only** — `BETWEEN 0 AND 43200` — and deliberately does not encode the floor | The rule existed in legacy and the migration was not carrying it: the default was 120, the ETL never sets the column and no endpoint could, so every migrated offering permitted booking two hours out against a rule of twenty-four. Invisible, because 120 is a valid value and every count reconciled. **The product rule is not in the `CHECK` on purpose**: `booking_policies` is already in the canonical package, so moving the 24–72 range there must be a config change rather than a migration — a database refuses what is impossible, an application refuses what is disallowed, which is the same split #100 makes. A `CHECK` at 1440 would also have forbidden every fixture from building a zero-notice offering, which is how 54 call sites exercise the slot maths. **Diverges from canonical**, which specifies `DEFAULT 120`; recorded here rather than as an ADR, following #102, where canonical says `NULL = inherit` and we shipped `NOT NULL` | When `booking_policies` lands, the 24–72 range moves there and this row points at it. The sanity ceiling is 30 days so it never becomes the thing blocking a product decision — the 72-hour ceiling is already expected to rise |

| 153 | **A converted vocabulary uses `str_enum()` from `infra/db/types.py`, never a bare `Text` column.** The `TypeDecorator` stores `text` and hands Python back the `StrEnum` member; `pg_enum` is for the types not yet converted, and every column is in exactly one of `PG_ENUM_TYPES`, `TEXT_CHECK_ENUMS` or `UNCONSTRAINED_ENUMS` | `pg_enum` was doing two jobs, and only one was obvious. The dialect-level `ENUM` also **coerces rows back into the class on read**, which plain `Text` does not — so `Mapped[ApprovalStatus]` over `Text` type-checks clean and yields a `str`. `==` still works, because a `StrEnum` member equals its value, so the defect hides; `is` does not, and `may_self_resume` compared identity. Converted naively it returns `False` for every mentor and a self-paused mentor can never resume — fails closed, so not an exposure, but a feature that stops working with a green suite. **Not `sqlalchemy.Enum(native_enum=False)`**, the one-argument version: it emits `VARCHAR(n)` sized to the longest member, so a longer value later is a column type change and a table rewrite — exactly the rigidity #100 exists to escape — and it hands the `CHECK` to SQLAlchemy when the point of #100 is that the constraint is altered in one statement | A future SQLAlchemy makes `Enum` render real `text` with a caller-owned `CHECK`, at which point the decorator is the framework's job rather than ours |
| 105 | **A migration copies the conversion template; it never imports a shared helper.** The reusable asset is the verification — `test_every_converted_enum_has_a_check_naming_its_values` and `test_the_database_agrees_with_how_each_vocabulary_is_declared` assert the outcome for every converted column | Decision #43 already says no migration imports a live symbol, because a later edit would change what an old revision does on replay — and a helper emitting `ALTER COLUMN … TYPE text` is behavioural, so it carries that hazard in full. The duplication argument is real and is answered one level up instead: a helper only guarantees the migrations that called it, while an outcome test also catches the hand-written one that got it wrong. Eight nearly-identical migrations that each say plainly what they do is worth more than eight that defer to a function, most of all for step 8 at 2am | Never for this conversion. A future migration family with genuinely dynamic DDL is a different question and gets its own row |
| 106 | **`mentor_profiles.requires_booking_confirmation` is the authority, `NOT NULL`, and `session_type_booking_configs.requires_booking_confirmation` is a nullable override where null means inherit.** The fan-out in `profile_writer` is deleted; the mentor's toggle writes one row. The ETL writes the mentor column and leaves every migrated offering inheriting | **The premise of #88 failed, not any one placement.** One fallback covering three fields, and each went somewhere else on contact: `meeting_venue` became per-offering and `NOT NULL` (#102), `custom_meeting_url` was deleted because nothing read it, and this one returns here. #88's argument against a nullable boolean — *no room for a third state, so null is indistinguishable from false* — was right about inheriting from a **primary offering**, because a mentor may hold live offerings and no primary, so the chain had a reachable empty bottom. **A mentor row always exists**, so the terminus cannot be missing, which is the whole difference. Two writers for one column is the duplication non-negotiable #8 calls a defect, and the fan-out was the second. The migration seeds the mentor from `bool_or` over their non-deleted offerings and *then* clears the offerings — reversing those two is silent, so a test writes an offering at `true` under the prior revision and upgrades over it | The override gains a producer — an offering-management endpoint that sets one deliberately, per offering. Nothing reads the override column until then (#21), and a `COALESCE` written before anything can write it would be a mechanism with no caller |
| 107 | **`mentor_profiles.primary_session_type_id` is dropped, with `trg_refuse_retiring_a_primary_offering`.** Retiring an offering — `deleted_at` or `is_active = false` — is refused by nothing. `default_meeting_venue` is **removed from `MentorProfileRead`**, not made permanently null | D88 gave the pointer two jobs and both are gone. The fallback lost its last member under #106; the other — *the offering a mentee lands on, which also drives display order* — was **never real**: nothing lands a mentee on it and nothing orders by it, because `_live_session_types` orders by `name`, unique per mentor among live rows and therefore a *total* order. Measured before deciding: three consumers in production code, and after #106, zero that need it. Venue is dropped rather than nulled because a permanent null asserts *this mentor has no venue*, a claim about the mentor, when venue is a property of an offering — the owner reads it per offering in `/me/session-types`. This removes a field from a shipped response and is breaking, deliberately. The guard's **five** tests are deleted with a record of what would bring each back, in `tests/integration/test_offering_retirement.py`, which asserts the inverse — that retirement is now refused by nothing, which is what the `PATCH` and `DELETE` endpoints are built on | A column referencing `session_types` returns. The table cycle, the three-step ETL write order it forces, and a guard that a foreign key cannot provide all come back together — they are properties of that reference, not of anything the loader or the endpoints do |
| 108 | **A mentor's conferencing options are their own table, and an offering references one through a composite key.** `mentor_conferencing_options` — one row per provider per mentor, exactly one default, a symmetric `custom_url` `CHECK`. `session_types.conferencing_option_id` is nullable, null meaning *use my default*, and `FOREIGN KEY (mentor_user_id, conferencing_option_id) REFERENCES (user_id, id)`. `session_type_booking_configs.meeting_venue` is dropped. The vocabulary is a new `ConferencingProvider` — `google_meet`, `daily`, `custom` — **not** `MeetingProvider`, which keeps `zoom` for history | `meeting_venue` was a *label*, and half its vocabulary named capabilities we do not have: `zoom` has no integration and nothing can mint a link, `custom` needs a URL and `custom_meeting_url` was deleted. Two of four values could not produce a joinable session, and one column could not distinguish *which provider* from *whether this mentor can host on it*. **The key is composite because a single-column one is satisfied by any option row, including another mentor's** — the composite version makes cross-mentor reference unrepresentable rather than merely refused, and `session_types` already carries `mentor_user_id` so it costs nothing there. Resolution is **three** steps, not two: seeding every mentor a default makes the third look unreachable, and *"it cannot happen because creation always sets it"* is exactly what failed for `primary_session_type_id`. `SessionTypeRead.meeting_venue` is required, so a resolution that can return null is a 500 waiting. The `CHECK` is symmetric where `mentor_profiles`' ran one direction and permitted `custom` with no URL. **Migrated `custom` offerings are quarantined** — no URL exists to carry, so they load on `google_meet` and are reported, on the #81 precedent | `zoom` or `teams` gain a connection behind them — that is what `external_account_id`, `status` and `connected_at` are declared for, and the point at which they join `ConferencingProvider`. Until then a mentor must not be able to select something that cannot host |
| 109 | **`POST` and `PATCH /api/v1/me/session-types` write the offering and its booking config in one transaction, and are scoped by `session_type_of()`.** `min_notice_minutes` carries the product range at the Pydantic boundary — `Field(ge=1440, le=4320)` — while the column keeps its sanity-only `CHECK`. `meeting_venue` is **not writable**; a new offering leaves `conferencing_option_id` null and resolves. A non-mentor gets `404`, not the empty page the read gives | Two rows because `/slots` and both read paths inner-join the config, so an offering without one is invisible everywhere and unbookable — and nothing writes a config on its own, so no endpoint could repair it. `session_type_of()` rather than a flag on `session_type_is_live()`: the live predicate decides what is **bookable** and `slot_store` spreads it, so a mis-defaulted `include_inactive` makes deactivated offerings bookable against #90, silently and one keyword away — a predicate taking no argument cannot carry that mistake. Editing a switched-off offering is the ordinary case and is what switching it back on requires. The notice range sits at the boundary per #104: a database refuses what is *impossible*, an application what is *disallowed*, and moving the range to `booking_policies` must be a config change rather than a migration. Venue is unwritable because nothing lists or creates `mentor_conferencing_options` yet, so a value here could only be a provider name this endpoint would have to turn into a row, inventing a `custom_url` it cannot ask for (#21). The duplicate-name `409` is translated off `ix_session_types_mentor_name` rather than re-implemented — checking first and inserting after is the race the index exists to win | A surface for managing conferencing options ships, at which point `meeting_venue` becomes writable as a reference to one of them. Draft-to-publish arrives: it is a transition with side effects and gets its own endpoint, never `PATCH {status}` |
| 110 | **`DELETE /api/v1/me/session-types/{id}` soft-deletes, and refuses with `409` while `LIVE_STATUSES` sessions are booked on that offering — carrying no machine-readable reason code.** Terminal sessions do not hold an offering open. The check is application-level, inside the write transaction | **The reason code is dropped, and its premise is why.** The plan settled *yes, the 409 carries one*, justified by *two refusals a client cannot otherwise tell apart* — the primary offering, and sessions still booked. #107 deleted the first, so there is exactly one refusal and nothing to distinguish it from. The envelope renders `type: "about:blank"` for every error and has no reason-code mechanism, so adding one would change a shared contract to serve a single caller, against #21. **Soft, never hard**: `sessions.session_type_id` is `RESTRICT`, so an offering that was ever booked cannot be hard-deleted at all, and the row is what a past session still points at. `LIVE_STATUSES` is **reused** from `models/sessions.py`, not retyped — it is the predicate behind the double-booking exclusion constraint and three partial indexes, and a predicate inside a `text()` string is not a symbol any linter can bind (#8). **The check is application-level and races**, which is stated rather than hidden: a soft delete is an `UPDATE` so `RESTRICT` never fires, a `CHECK` cannot span tables, and #107 removed this schema's only business-rule trigger — a booking landing between the check and the update leaves a live session on a deleted offering, whose mentee can still read it because `GET /sessions/{id}` does not consult the offering | A second refusal exists. Then the `409` needs a machine-readable reason and the `type` slot in the RFC 9457 envelope is where it goes — additive, so nothing shipped here blocks it. Or the race stops being survivable, which would mean a trigger on the `#107` precedent rather than a wider application check. **Reopened and answered 2026-08-22, on its own terms**: `POST /reviews` has two refusals — a session already reviewed, which is terminal, and an offering reviewed inside its interval, which resolves with time — so the `type` slot is now in use. `TYPE_BY_ERROR` in `api/errors.py` maps the error class to a relative URI, mapped in the API layer rather than raised with the error because that module is where transport concerns live. Relative rather than absolute: an absolute URI names a host that differs per environment or a docs site that has to exist. Only a refusal a caller must respond to *differently* earns one — a type on every error would publish a vocabulary nobody maintains |
| 111 | **`session_types.category` becomes `service_offering_id`, a nullable FK to the six-row taxonomy, and `application_stage` becomes a five-value closed set with `custom_stage_label` tied to `other` by a symmetric `CHECK`.** Both are **published** on the public read, as a `code`/`display_name` pair for the taxonomy. `sessions.topic` is *not* converted | The columns were withheld because publishing free text *"would commit this contract to a shape nobody has designed"*; the UI designed both, so the argument **lapsed** rather than being overruled — the reasoning still governs the next undesigned column. Free text on `category` was also #53's failure reintroduced one table along: it is the *matching axis*, and "SOP help", "Statement of Purpose" and "sop" match nothing. **Not named `category`**, because `service_offerings` has its own `category` column — a display grouping — so an FK by that name points at a different `category` one join away; `mentor_service_offerings` and `mentee_goal_needs` set the precedent. The `CHECK` is symmetric because the one-directional form is satisfied by a null label and never requires the payload it exists to require — the identical hole in `ck_mentor_profiles_custom_url_requires_custom_venue`, confirmed by rewriting it that way and watching one test fail. Enforced twice: `422` at the boundary, `CHECK` for the ETL and `psql`. The taxonomy join is **outer** — every offering is unclassified today, and an inner join fails 19 tests by dropping the whole population. See ADR 0022 | The five stages meet real data. They came from mock screens, no row holds one, and `other` is the hatch that makes being wrong survivable. `sessions.topic` converts with the booking work, where it is a data migration over 1,073 migrated rows rather than a schema change over an empty column |
| 112 | **The intake stack is four tables landing together**, in their own `models/intake.py`, following the canonical package rather than diverging from it. `session_type_question_options` and `QuestionType.MULTI_CHOICE` ship with **no writer**. An answer restricts its question while a question cascades from its offering, and `session_type_questions` carries `deleted_at` because of it | Half the stack is worse than none: a question definition with nowhere to store an answer is a schema asserting a feature nobody can use, which is why #21 deferred all four rather than some. **The unused option table is the interesting call.** #21 would normally defer it and the UI's intake screens use two of the three question types — but the package is canonical (ADR 0007), so dropping one is an *undeclared divergence* rather than a deferral, and **#100's sub-rule against an unused vocabulary value has been retired**: it was justified by `ALTER TYPE ... ADD VALUE` being permanent, and #107 recorded that it "does not survive its removal". Being early now costs a migration. The deletion asymmetry is ADR 0013 applied literally — a question is meaningless without its offering and records no fact, an answer is *evidence of what was asked* — and it is what forces `deleted_at` onto the question: without it a mentor editing their form is refused by every answer ever given. `exactly_one_answer_form` **sums rather than chains**: `a IS NULL OR b IS NULL OR c IS NULL` permits an answer carrying nothing, verified by writing it that way and watching two tests fail. Own module because `sessions.py` is five models and past #54's line tripwire | A question type needs a shape these three cannot express. `multi_choice` gains a writer, at which point the option table stops being early. The cross-table rule nothing enforces — that an answer's form matches its question's type — needs a trigger or a boundary check, and the boundary is where it goes |
| 113 | **A mentor's intake questions are `/api/v1/me/session-types/{id}/questions`, nested under the offering, capped at five, and `multi_choice` is not selectable.** `MAX_QUESTIONS` lives in `domain/`, deletion is soft, and both ids are in the `WHERE` on every write | Nested because **the offering is what carries ownership**: a flat `/me/questions/{id}` would look the question up and check afterwards, which is the shape non-negotiable #5 refuses — and scoping a write on `question_id` alone lets any mentor edit any question by guessing an id, which is what the cross-offering test catches. **Five is a product rule with no column to hold it**: "at most five rows in a group" is not a `CHECK`, which sees one row, nor a unique index, which enforces distinctness rather than cardinality — the only database mechanism is a counting trigger, which is what #107 removed. So it is counted in the store and **races**, and the loser leaves one extra question rather than corrupting anything. The count is of **live** questions, so deleting frees a slot; counting every row strands a mentor who added and removed five. `MAX_QUESTIONS` is in `domain/` because the **layer check said so** — `api` importing it from `infra` was a boundary violation, and a product rule belongs in the layer that owns meaning. `multi_choice` is refused at the boundary because nothing can create an option, so the question would be unanswerable — the selectable set is narrower than the column's, exactly as `ConferencingProvider` is narrower than `MeetingProvider` | Option management ships, which lifts the `multi_choice` refusal. The limit moves to `booking_policies` or its successor if it ever needs to vary per mentor, at which point it stops being a constant |
| 114 | **`session_type_scheduling_windows` replaces general availability for the offering that has them; an offering with none uses `availability_rules`, unchanged.** `availability_exceptions` still subtract, always. A mentor with windows and no general availability is bookable. The exclusion constraint is scoped to the **offering**, not the mentor | The screen's copy says *restrict*, and its own example shows why intersecting is wrong: a deliberate evening window against normal working hours yields **zero** slots and an empty calendar with nothing to explain it. Exceptions are untouched because windows replace *availability*, not *unavailability* — derived rather than stated, so it carries its own test. The table mirrors `availability_rules` column for column so `bookable()` needs no second code path: a parallel slot algorithm would be the same maths written twice, and the copy that drifts is the one nobody reads. **The risk was never the table** — every offering in existence has no windows, so the whole population depends on the fallback, and removing it fails 14 tests including the entire existing slot suite. See ADR 0023 | The offering read model must say **which mode** it is in — precedence only true in the backend is precedence nobody can act on. That ships with the surface that manages windows, which does not exist yet. #89's shared `availability_schedules` parent is the right shape when a mentor wants one named schedule across offerings, and stays open |
| 115 | **`POST /api/v1/sessions` is the booking endpoint, and `Idempotency-Key` is required on it.** Legality is a membership test against `list_slots`, not a second copy of the rule; `starts_at` must match an offered instant to the second, and every reason it might not is one `422`. Status is `COALESCE(config.requires_booking_confirmation, mentor_profiles.requires_booking_confirmation)`, and `duration_minutes` is snapshotted onto the session. A retry replays the stored body with `Idempotent-Replayed: true`; the same key with a different body is a `422`; a key in flight is a `409` (ADR 0024) | The header is required where Stripe recommends it, because the client here is a phone on a dropping connection rather than a server written once — optional makes the guarantee opt-in for exactly the caller who needs it, and relaxing a required header later breaks nobody while requiring an optional one breaks everybody. Legality goes through `list_slots` because re-deriving it is non-negotiable #8 at its most expensive: the copy that drifts silently offers or refuses the wrong hour, and the two would be tested apart so neither test would notice | The cancellation policy lands and confirming or cancelling need contracts of their own — those are new rows, not edits to this one |
| 116 | **`idempotency_keys` is unique on `(user_id, key)` with `NULLS NOT DISTINCT`, not on `key` alone**, and the table is a replay cache in front of `sessions_no_mentor_double_booking` rather than the control (ADR 0024). Expiry is enforced by the queries — an expired row is reclaimed in place — so the retention job keeps the table small and nothing depends on it having run | The row holds a stored *response body*, so the lookup must be scoped to the caller; once it is, a global key space is a defect rather than a stricter rule, because the second caller's scoped select finds nothing while their insert collides. Calling the table the control is the belief that gets the constraint relaxed later: what actually makes a double booking impossible is that two bookings for one mentor at one instant overlap by definition | Never for the scope. The TTL is a constant, not a decision |
| 117 | **The four transitions are one table in `domain/sessions.py`, not four endpoints with four implementations.** `accept` and `decline` are the mentor's, `withdraw` is the mentee's, `cancel` is either party's and only from `confirmed`. The wrong party gets **404**, not 403 — the action's URL does not exist for them, which is what `require_admin` already answers a non-admin; the right party in the wrong state gets 409. The ten-minute cancellation cutoff lives in `domain`, not in a trigger. Reason codes are restricted per actor and a disallowed one is a 422 | A rule enforced in four places is enforced in three: "a mentee may never accept their own request" is the kind of thing that is right in the endpoint somebody was looking at. The cutoff is in `domain` because the project already settled where a rule like this goes — the database refuses what is *impossible*, the application refuses what is *disallowed* — and the plan's proposed trigger correctly ruled out a `CHECK` (predicates must be `IMMUTABLE`, `now()` is `STABLE`) without that implying a trigger; a trigger would make every change to the number a migration. The reason codes are restricted because `SessionReasonCode` says they drive refund policy, so a mentee free to send `mentor_unavailable` can claim a refund by choosing a value | Refund policy lands and needs a code no party may currently give — that is a new row in the table, not a change to this rule |
| 118 | ~~**`NEVER_AGREED` — `declined`, `withdrawn`, `expired` — does not block a slot; `cancelled` still does.**~~ — **REVERSED by #151.** The constant is now `FREES_THE_HOUR` and `cancelled` is in it | The original argument is preserved in #151, which explains why it failed on its own terms: it correctly identified the denial-of-service shape for three statuses and then exempted a fourth with the same shape, on a guess about the mentor's reason | Never by argument — it already lost once |
| 119 | **The join window is `starts_at - 5min` to `starts_at + 15min`, half-open, and it is where attendance is reported and where the outcome becomes decidable.** `POST /sessions/{id}/join` marks the **caller only** present; `settle_attendance` then derives `completed` (everybody came) or `no_show` (anybody did not) once the window shuts, set-based and idempotent, driven by `scripts/settle_sessions.py` on an hourly GitHub Actions schedule. It settles `confirmed` sessions only, so migrated rows are never rewritten | Half-open is what stops joining and settling claiming the same instant — a sweep on the boundary would mark somebody absent in the second they were still allowed to arrive, and nothing later can undo it because joining after the shut is refused. Scoping the arrival to `user_id = actor_id` matters because attendance drives *both* parties' reliability figures, so marking somebody else present is editing their record. Set-based because a per-session loop makes a partial settlement reachable — a session saying `completed` while its participants still say `pending`. Migrated rows are left alone because `session_stats` measured three sessions already `completed` with somebody absent: they are the record of what the legacy app believed, and correcting them destroys the evidence that the figures disagree | The window becomes a mentor preference, which is why the two ends are separate constants. A meeting provider ever reports attendance to us — then the client-reported signal stops being the only one |
| 120 | **The attendance rate is one function taking the side as a parameter**, in `session_stats.attendance_rate(user, side)` with `MENTOR` and `MENTEE`. The mentee's travels on `SessionRead` as `mentee_attendance_rate`, correlated per row; the mentor's stays on their public profile. **Null means no data and the API never renders it** — a client shows "New mentee" | Two copies of the arithmetic is where one of them stops counting `no_show`, which is why `delivered()` already takes its mentor as a parameter. The side is load-bearing and the two numbers are not interchangeable: a person who hosts diligently and books unreliably has two records, and pooling them lets the first flatter the second on the card where a mentor decides. **The rate is on the session rather than on a mentee endpoint** because the question is asked on the request card, and a second round trip per card is worse for the same work. Null is not rendered here for the reason `PartyRead` gives about a missing name: substituting words is a display decision made in the wrong layer, in one language, that no client could change | A mentee-facing profile needs the figure in its own right — then it is a second reader of the same function, not a second definition |
| 121 | **`respond_by = starts_at - 6h`, stored, on confirmation-required offerings only; null where nothing awaits an answer.** An hourly sweep in `settle_sessions` writes `expired` with `actor_type = system` and `expired_no_response`, which frees the slot because `expired` is in `FREES_THE_HOUR` (#151). The deadline stays on the row after the mentor answers | **Stored rather than derived because the exclusion constraint covers `pending_mentor_approval`** — an unanswered request holds the mentor's hour indefinitely, nothing writes to an abandoned row so a trigger cannot free it, and a constraint predicate cannot carry the deadline at all (`IMMUTABLE` required, `now()` is `STABLE`). **Six hours rather than twenty-four because of the booking floor**: the mentor's time to answer is `(starts_at - booked_at) - W` and `booked_at` is at best `starts_at - min_notice_minutes`, so against the 24h floor a 24h window leaves **zero** and every default-configured offering would expire requests the instant they were made. Six leaves eighteen. Measured backwards from the session because the guarantee is to the *mentee* | `W` becomes a mentor preference — then it is bounded below by that mentor's booking notice, and the two cannot be set independently |
| 122 | **Reminders fire at fixed distances from `respond_by`** — on booking, 24 hours before, 12 hours before — and the 24-hour one is reachable only when the mentee booked at least 30 hours out. **Settled and unbuilt** | Deadline-relative is the only well-formed reading: the minimum gap from booking to deadline is 18 hours, so a reminder measured 24 hours *forward from booking* would fire after the deadline had already passed on every minimum-notice request. Unbuilt because it needs a notification channel that does not exist — and the deadline ships without it, because freeing the slot is the half that does not depend on being able to tell anybody | The notification work lands, or `W` moves and changes which reminders are reachable |
| 123 | **Every attendance outcome records how it was reached**, as `AttendanceEvidence` in `session_events.metadata` — `reported` today, `observed` once a provider sends per-participant join and leave. Written before anything reads it | It cannot be reconstructed: a session settled today cannot later be re-examined for whether anybody observed it, so the fact is recorded when the outcome is decided or it is lost — the same argument that keeps `respond_by` on an answered row. It is what lets a payout rule one day require observed attendance **without a second status and without re-judging history**, and what stops `completed` quietly meaning two different things either side of the first provider integration. The enum lives in `domain/attendance.py` rather than `domain/enums.py`, which holds only vocabularies a `CHECK` constrains; this one is a JSONB value and filing it there would imply a column that does not exist | A provider's report turns out to be untrustworthy, and `observed` stops meaning what it says |
| 124 | **`custom` stays in `ConferencingProvider` and is refused at the write surface when one exists** — not removed from the enum | Removing it is not the one-line change it looks like: `ConferencingProvider` is a column type through `str_enum()`, so dropping a member means a migration, a `CHECK` rewrite, breaking any stored `custom` row, and breaking the read schemas typed on it. And there is nothing to refuse yet — `mentor_conferencing_options` has **no write surface at all**, so no `custom` option can be created through the API today. The pattern when that surface arrives is `SELECTABLE_TYPES` in `api/schemas/intake.py`: the column keeps the value, the boundary narrows what may be chosen, and the subset widens when the missing piece lands. That is also what makes hiding it a *validation* rather than a commitment | Two providers prove insufficient and a mentor genuinely needs their own room — the shared-room problem in `Session.meeting_url` is the cost to weigh |
| 125 | **Multi-value configuration is one env var holding a JSON object or `key=value` pairs**, typed `dict[str, str]` with `NoDecode` and a `mode="before"` validator accepting both spellings — the `cors_origins` shape. Provider template ids live in `EMAIL_TEMPLATES` and `WHATSAPP_TEMPLATES`, **two maps rather than one**, resolved against the `Notification` vocabulary in the adapter | Neither Railway nor FastAPI Cloud has a JSON-config feature; both set `KEY=value` strings, so what makes a map work is pydantic-settings rather than the platform. `NoDecode` is load-bearing: without it the value is decoded *inside the settings source* before any validator runs, and a malformed entry fails as `SettingsError` naming neither the value nor the shape — which cost a deploy to diagnose once already. The keys are **not** typed on the enum because `core` may import nothing; the adapter resolves and raises `ConfigurationError`, which is where that error's docstring says it belongs. Two maps because a message exists on email long before WhatsApp, and one map would make the absent half look like a mistake rather than a phase | A third channel arrives, or template ids become secret |
| 126 | **A session's venue is provisioned when it becomes `confirmed`, which is two call sites — booking for an auto-confirming offering, `/accept` for one that waits — and the guard lives in `provision_meeting` rather than at either.** The provider decides two *independent* things, held as a table in `domain/meetings.py`: `google_meet` asks the calendar for a conference and creates no room, `daily` creates a room and must **not** ask, `custom` creates nothing and reuses the mentor's URL | A reader who assumes *needs a room* and *wants a conference* are opposites gets Meet right and Daily wrong — and asking for a conference on a Daily session puts **two links on the event** with the invitee clicking whichever renders first, which errors nowhere and is found when somebody joins an empty room. The guard is central because booking calls this for every session and only some confirm: a check at the caller is a check somebody must remember twice, and forgetting mints a room for a request that may be declined — on a metered provider, one somebody pays for. **The first version had exactly that bug**, documented in a comment and absent from the code, and a test caught it. A failure to provision does not fail the booking: the session exists and the slot is held, where a booking refused because a third party was slow loses something unrecoverable | Group sessions, or a provider whose room and conference are the same call |
| 127 | **Provisioning resolves the venue through `session_type_store.resolve_venue`, sharing `_resolved_venue`'s `COALESCE` with the two read models** — offering's option, else the mentor's default, else `google_meet` | Provisioning must agree with what the mentee was *shown*. An offering listed as held on Daily that mints a Meet link is a contract broken silently, and two resolutions of one precedence is non-negotiable #8 in the form where neither copy looks wrong. The custom URL rides along in the same row because for `custom` the URL *is* the resolution, and a second query could pick a different option row | Never for the sharing. The fallback moves only when every mentor is guaranteed an option, which `primary_session_type_id` already proved is not a guarantee anybody should rely on |
| 128 | **Every Daily room for a session is created `private`, without exception**, and the room URL reaches a participant only through the platform. The webhook (`participant.joined` / `participant.left`) feeds the *live* view; `GET /meetings` feeds *settlement*. They are complementary, not alternatives | A public room makes the withheld link pointless and the recorded join meaningless — if the URL alone admits somebody, pressing our Join button becomes optional and attendance goes back to self-report. Settlement needs no push because the meeting record accumulates every participant, so an hourly sweep misses nobody and the spike measured no lag. The live view cannot be served by polling: the arrival a waiting party sees today is our own Join press, which records an *intention*, so somebody who presses it and never reaches the room leaves the other party looking at a lie — and "true within a second" needs a push. An earlier reading concluded no webhook was needed at all; it had analysed settlement and stated the answer as though it covered both | Never for private rooms. The webhook only if a live view stops being a product requirement |
| 129 | **Every OpenAPI tag on a route carries a description, and every description belongs to a tag in use** — asserted both directions in `tests/unit/test_openapi_tags.py`. A mentor's own offerings and their intake forms are the `session-types` tag, not `users` | Three tags — `public`, `availability`, `sessions` — shipped undescribed because nothing compared the two lists: the spec still rendered, the endpoints still worked, and the only symptom was an empty heading in documentation nobody generates in CI. The reverse direction rots more quietly, because a description left behind by a rename documents a section that no longer exists, which is confidently wrong rather than merely thin. The split follows #64's *everything else takes its domain name*; `me_session_types.py` said at the time that a tag "may still earn its place once `DELETE` joins them", and it has — eight endpoints against `users`' own handful | A tag grows past one domain, which is the same test applied again |
| 130 | **`GET /api/v1/admin/mentors/{id}/history` filters by `status` (repeatable) and by a half-open `[since, until)` window of UTC instants**, validated at the edge | A log readable only whole is a log nobody reads past the first page, and *"why was this mentor unlisted in March"* filters both at once. Half-open so two adjacent ranges partition the log — a closed upper bound returns a boundary event in both, and a reviewer paging month by month counts it twice. Instants rather than dates because this log has **no owning mentor whose day it could mean**, unlike `/slots`, and the reviewer's zone is not something the server knows; a naive value is refused rather than guessed. Validated at the edge, again unlike `/slots`, because both bounds are absolute values the caller either sent or did not and nothing downstream can change them | Pagination arrives — then the window and the cursor have to agree on ordering |
| 131 | **Two email senders, split by use case: Emailit carries the Supabase auth code, marketing and subscription; Loops carries every transactional message this service sends.** Each sends from its own subdomain with its own DKIM key, and template ids are prefixed with the sender (`loops:tmpl_abc`) so a message can move providers by configuration alone (ADR 0025) | Not the split a clean sheet produces — it puts auth beside *marketing*, the class that generates complaints, and apart from the transactional mail it most resembles. Chosen on a constraint honestly stated rather than a principle: the templates exist in Loops and cannot be exported, rebuilding them competes with migration work, and Emailit is already warmed and already carrying auth. **The subdomains are what make it safe** — reputation attaches to the `From:` domain and its DKIM signature rather than to the vendor that relayed it, so two senders on one domain share one reputation and the split is decoration. The prefix is the incremental path out, a message at a time | Templates get rebuilt, at which point one sender is strictly simpler |
| 132 | **A message caused by somebody goes to the party who did not cause it; a message caused by time going past goes to both.** `expired` is the single exception and goes to both, because no person produced it. The two confirmations are the same rule applied twice — auto-confirm tells the *mentor*, acceptance tells the *mentee* — and carry different content (ADR 0025) | Legacy told both parties everything, which tells each of them something they already know: after an auto-confirming booking the mentee is looking at a confirmation screen, and after an acceptance the mentor has just clicked the button. One rule generates almost the whole taxonomy, so the vocabulary is derivable rather than a list somebody maintains — and the exception is principled rather than forgotten | Group sessions, where "the other party" stops being one person |
| 133 | **Reminders are QStash callbacks published when the session is created, and the callback re-reads the session before it sends** (ADR 0025). Nothing is ever cancelled | The hourly sweep cannot deliver a 30-minute or 5-minute reminder, and GitHub Actions schedules are documented as delayable under load — so a tighter cron is unreliable however short the interval, and scans the whole table to find almost nothing. QStash is vendor-neutral HTTP, so #13's exit criterion (no *platform-native* queue) is untouched, and it is already in the vendor allowlist. **Re-checking at delivery is what removes the trap**: scheduling ahead normally obliges four transitions to unschedule, and the bug is the message that fires for a session called off through the one path somebody forgot. Re-reading makes that unreachable rather than handled | A reminder ever needs to fire for a session that no longer exists, which is a contradiction |
| 134 | **A Daily meeting token is minted by `/join` and never stored.** `record_arrival` returns the venue and the room's name; the dependency mints a per-participant token — our `users.id`, mentor as owner, `nbf`/`exp` matching the room — and returns `<room url>?t=<token>`. Every other venue returns the stored URL unchanged | A private room refuses anybody without a token, so recording an arrival and handing back the stored address sends the participant somewhere that turns them away — which looks like a broken platform rather than an unfinished one. **Storing the token instead would put two live bearer credentials per session into the database and every backup**, outliving the session they open; minting costs one request at a moment somebody is already waiting for a page. The mentor is the owner because on Daily that is who may admit, mute and end, and one shared token would let a mentee end their mentor's session. Requesting a conference for a non-Daily venue is the mirror failure — see #126 | A provider whose rooms are addressable without a credential |
| 135 | **A venue failure never fails the request that provoked it.** Every Daily error — non-2xx, network, or a 200 carrying nonsense — becomes `VenueUnavailableError`, and both callers carry on: `provision_meeting` leaves `meeting_url` null, `/join` returns `meeting_url: null` with the arrival still committed | The caller's decision is identical for every cause, so one error type is the honest shape. A booking refused because a third party was slow loses a slot that cannot be recovered, where a link can be minted later; and a 500 on `/join` fails a request that has *already* recorded the arrival, for somebody trying to attend a session **right now**. The 200-carrying-nonsense case is the one `raise_for_status` alone lets through, which is why the adapter validates the shape | Never. If a venue becomes mandatory, that is a booking-time precondition rather than a failure mode here |
| 136 | **A message is queued in `outbox_events` inside the transaction that caused it, and sent later by the drain in `settle_sessions`** — one row per recipient, the recipient held as an *id* rather than an address (ADR 0025) | Sending inline fails twice at once: a booking would block on a third party, so a slow provider makes a slow checkout; and a crash between the commit and the send loses the message with nothing recording it was owed. The outbox makes *this happened* and *this person was told* one transaction and one retryable unit. One row per recipient so a send failing for one person is retried for that person, where a single row would have to choose between resending to everybody and losing the rest. An id rather than an address because the drain resolves it at send time — somebody who changes their email between the enqueue and the send is written to at the new one, and a stored address puts a second copy of a contact detail in a table nobody thinks of as holding one | Never for the outbox. A message needing sub-minute delivery, which is the reminder case and is why those are QStash callbacks instead |
| 137 | **Retries are bounded at 5 and a missing address is `skipped`, not `failed`.** The outbox row's own id is the provider idempotency key | An unbounded retry is an outage amplifier: a provider refusing everything would be asked again by every row on every sweep, forever. Five hourly attempts spans a working day, and a row at the bound stays `failed` with its error so what was owed and never delivered is answerable from the table rather than a log. **Skipped and failed are different questions** — failed means the provider said no, skipped means there was never anywhere to send it — and conflating them burns the retry budget on a message that can never be delivered while hiding a data problem inside a delivery one, which matters now because WhatsApp can reach nobody. The row id as the key works because it exists exactly once per message per recipient, so a retry after a timeout replays the provider's answer rather than sending twice | A provider without an idempotency header — then the key moves into the payload and the adapter dedupes |
| 138 | **`addToAudience` is explicitly `false` on every Loops send** | One flag, and it decides the bill and the compliance posture. True turns every transactional recipient into a Loops *contact*; the free tier caps contacts at a thousand against roughly twelve hundred migrated users, so it would move the account onto a paid plan **and** quietly change what "transactional-only" means for unsubscribe handling. Set explicitly rather than left to the provider's default, because a default is a thing that changes without telling you | Loops becomes the marketing sender too, at which point the audience is the point |
| 154 | **The calendar is the platform's own Google account, configured as three values plus a calendar id — not a per-mentor grant.** Both parties are invited as guests and neither completes an OAuth flow. Plain REST through `httpx`, not `google-api-python-client` | The per-mentor belief was wrong and expensive: `meetings.py` claimed the adapter needed `calendar_connections`, and acting on it would have meant building a table to unblock something that never needed one. That table records a mentor's `calendar.freebusy` grant, which buys *conflict detection* — later and separate. REST because the SDK brings a discovery mechanism and a large dependency tree to do what two POSTs do, and `DailyRooms` beside it already works that way, so one reader learns one pattern. **All three or none**: a refresh token is useless without the client that minted it, and two of three is a half-finished setup where failing to `NullCalendar` is quieter than failing every booking with an OAuth error | Conflict detection arrives, which needs the per-mentor grant and is `calendar_connections`' actual job |
| 155 | **A session that ends before it happens releases its calendar event** — declined, withdrawn, cancelled, expired; never `completed` or `no_show`. The stored id is cleared **only on a successful removal**, and a failure never fails the transition | `external_calendar_event_id` was written by provisioning and read by nobody: harmless while no event existed, and a live defect the moment one does — a cancelled session would leave a meeting in both parties' calendars forever with an invitation nobody withdrew. Not released on `completed`/`no_show` because the session happened or its time passed, and the event is then a true record of what was scheduled. Cleared only on success so a failed removal keeps the only handle on an event still in somebody's calendar. The transition still succeeds because the session is already off, and refusing to record that because a third party was slow leaves the two facts disagreeing — a stale event is recoverable by hand, a session cancelled in Google and confirmed here is not | Never |
| 139 | **A machine-called endpoint lives under `/api/v1/callbacks`, is authenticated by signature rather than a bearer token, and re-reads the state it is about before acting.** Verification checks the issuer, the destination (`sub`) **and a hash of the exact body**; an unconfigured verifier rejects everything | The first surface here with no user behind it, so the grouping matters: one of these among the authenticated routes is how somebody later assumes a token is present. **The body hash is the load-bearing check** — a valid signature proves the provider issued *a* token, and only the hash proves it issued one for *this payload*; without it anybody who observed one callback could replay its signature against a body of their choosing, and this endpoint queues messages. Unconfigured must **reject**: a public message-queueing endpoint that accepts everything is quietly open, where one that rejects everything is visibly broken | Never for the hash. A provider signing differently gets its own verifier beside this one |
| 140 | **Reminders are QStash callbacks published at booking, and nothing is ever cancelled** — the callback re-reads and does nothing for a request already answered. At most one per session per kind, enforced by a partial unique index on `(entity_id, event_type, payload->>'kind')` | Cancelling would make four transitions responsible for unscheduling, and the bug is the reminder that fires for a request answered through the one path somebody forgot; re-reading makes that unreachable rather than merely handled. **The index rather than the re-read is what stops duplicates**, because QStash retries by design and two callbacks arriving together would both see a pending request. ADR 0015 prescribes exactly this shape — a natural key re-declared as `UNIQUE` — and it is partial so no other message is constrained. A reminder whose moment already passed is **dropped rather than fired late**: at the 24-hour booking floor `t24` is already behind, and sending it would be the booking message again thirty seconds later | The schedule changes, which is a change to `REMINDER_OFFSETS` and nothing else |
| 141 | **An app carries its own settings on `app.state.settings`**, and dependencies read them through `_configured(request)` rather than calling `get_settings()` | `get_settings()` is an `lru_cache` over the environment, so a dependency calling it directly ignores whatever `create_app` was handed — which is how every test builds an app, and how a second app in one process would run on the first one's configuration. Found by a callback suite whose signing key was configured on the app and read from the environment: nine tests failed `401` with the key sitting right there. Follows the pattern `session_factory` and `token_verifier` already use | Never |
| 142 | **A third-party failure is `core.UpstreamError` and maps to `502`.** An adapter error class lives in `core/errors.py`, never beside the adapter that raises it | `api` may not import `infra`, so an error defined there is one the transport layer cannot name — it falls through to the unmapped-`AppError` `500`, which is a gap the table's fallback exists to *reveal* rather than a status anybody chose. This has now happened twice: `AuthenticationError` moved for it, and `VenueUnavailableError` sat unmapped through three PRs because no path let it reach a client. **502 rather than 503** because the fault is always upstream; 503 claims this service is unavailable and invites a retry against a request that will fail identically | A failure mode appears that is genuinely ours — capacity, a deliberate drain — which is a different class, not a different status on this one |
| 143 | **A credential belonging to a user is encrypted in the application with Fernet, in a column whose name says so.** The key is a setting; unset means the feature refuses rather than storing plaintext. Disconnecting **destroys** the credential and keeps the row | pgcrypto would put the key in the SQL statement, so it reaches the query log, the slow query log and `pg_stat_statements`; encrypting in the process keeps the key in memory and puts only ciphertext on the wire, and `cryptography` is already a declared dependency. The column name is the guardrail that a type cannot be — `refresh_token` and `refresh_token_encrypted` hold the same Python `str`, and only one of them makes a plaintext write look wrong. Refusing when unconfigured matters more than it sounds: the alternative is somebody's credential in the clear because a variable was forgotten, which is not a state worth being reachable | A managed secrets store arrives and holds these instead — then the column goes, not the naming rule |
| 144 | **An OAuth `state` is sealed, carries the user, and expires in ten minutes.** The callback takes no bearer token and re-derives the caller from the state alone | Without it an attacker completes their *own* provider consent against a victim's session and attaches their account to somebody else's — account takeover, not a theoretical one. Sealing rather than signing because there is no third party to interop with: only this service writes these and only this service reads them, so a JWT buys nothing and costs a second way to get an algorithm wrong. **Ten minutes is a consent screen, not a session** — long enough to read what is being granted and pick an account, short enough that a `state` captured from a browser history is useless. Replay *within* the window is accepted knowingly: the code it accompanies is single-use at the provider's end, so a nonce table would be a write per consent to close a window nothing can walk through | A provider appears whose code is not single-use, which is when the nonce table earns itself |
| 145 | **An outbound consent asks for one scope and never `include_granted_scopes`; the redirect is one constant used at consent and at exchange; and `access_type=offline` with `prompt=consent` are asserted by test, not by reading** | Every defect this shape has is in the *outgoing request* and none is visible in the response. Without `prompt=consent` Google returns `200` and an access token, and the connection works for an hour before stopping with nothing saying why. `include_granted_scopes` lets an earlier grant silently widen this one, so the narrow ask becomes a lie the consent screen already told. Two redirect strings that happen to agree fail only in production and only for the first user to try. A test asserting the scope against the *constant* is tautological — it must assert the literal, which a mutation caught here | Never. The provider's parameter names change, not the rule |
| 146 | **A third-party read that only *removes* options fails open**: unreachable means it contributed nothing, and the request proceeds. A read that *grants* something must fail closed | ADR 0004 already called free/busy advisory and said it must never be the thing preventing a double booking — the exclusion constraint is. Refusing a booking because Google was slow promotes an advisory check to an authoritative one and makes a third party's outage ours, on a public endpoint. The cost is named rather than hidden: during an outage a mentee can book over a real external conflict, which is exactly the position every mentor without a connection is in permanently, and strictly better than nobody booking at all. The asymmetry is the whole rule — this reasoning does **not** transfer to a check that would let something through | The read becomes the only conflict detection, which would mean the database constraint was removed — a different decision entirely |
| 147 | **Transient and fatal third-party failures get different treatment, and only the fatal one is written down.** A timeout, 5xx or rate limit is logged; a credential the provider has rejected marks the connection dead and clears the token | Treating them alike is wrong in both directions: re-consenting a mentor over a rate limit, or leaving a revoked grant retrying forever on every public slot render. The fatal write is what bounds the cost — once the status leaves `active` the mentor is invisible to the token lookup, so no further call and no further write happen. **It needs its own transaction**: a slot render never commits and a failing booking rolls back, so a failure recorded on the caller's session is one nobody ever sees — and it must equally never commit the caller's half-written booking. Caught by a test asserting the second request makes no call, which it did | Never for the distinction. Where the write happens changes if a connection-health job takes it over, which ADR 0004 asks for |
| 148 | **A `200` is not a success when the provider reports per-item errors inside the body.** Google's `freeBusy` answers `200` with `{"errors": [...]}` under the calendar that failed | Reading past it reports a mentor with a full diary as completely free — the exact inversion of what the call is for, arriving with no status code to notice and no exception to catch. The same shape already bit this codebase once at `conferenceDataVersion`, where a `200` silently dropped the Meet link. Two instances is a pattern: **assert the thing you asked for is present, rather than that the request succeeded** | Never |
| 149 | **A test asserting an aware `datetime` must assert its `tzinfo` separately when the point is the offset.** Equality compares instants, so `14:00+02:00 == 12:00Z` passes | A mutation removing `.astimezone(dt.UTC)` survived a test that looked like it covered exactly that. The value still compared equal and the type is called `UtcInterval` — holding a `+02:00` value in one is a lie regardless of how it compares, and the next reader who formats it rather than comparing it gets the wrong answer | Never |
| 150 | **A create and its matching cancel notify the same way, and a test pins the pair.** Whether a third party announces an action or the platform does, both ends of the same booking agree | Found live: `create_event` sent `sendUpdates=all` and `cancel_event` sent nothing, so Google emailed the invitation and stayed silent on the cancellation — the meeting vanished from the mentee's calendar with nothing said, and a mentee turns up to a session that is not happening. Each call read correctly on its own; only the *pair* was wrong, which is why no per-call test caught it and why the assertion has to compare them to each other. The same shape applies to any create/undo pair sharing a notification setting | Never for the symmetry. Which way it points is a product decision, and moving it means moving both |
| 151 | **A cancelled hour goes back on the grid, and a mentor who is not free records an *availability exception* instead.** `FREES_THE_HOUR` — `declined`, `withdrawn`, `expired`, `cancelled` — is what `slot_store._busy` subtracts, and **every member must also be absent from `LIVE_STATUSES`**. Session state is never where unavailability lives | #118 got three of four right and exempted `cancelled` on the reasoning that a mentor who cancels is probably busy. Both halves of that are true and neither makes the hour occupied. What it shipped was an hour hidden from the grid that `sessions_no_mentor_double_booking` — over `LIVE_STATUSES`, which excludes `cancelled` — would have accepted a booking for anyway, with the "deliberate release" its docstring promised never built. A mentee could book and cancel repeatedly and empty a mentor's calendar an hour at a time: the same mentee-driven denial of service `withdrawn` once was, which is why the pairing rule and not the reasoning is what this row asserts. **An exception rather than a flag on the session** because the mentor can see it, delete it, and already understands it — a private column would make the grid identical and the calendar a mystery. It also applies across every offering, which is what "I am not available" means | Never for the pairing. Whether the *default* releases is a product call, and it defaults to releasing because an hour offered while busy arrives as a booking that can be declined, where an hour withheld while free arrives as nothing at all |
| 152 | **A local window is stored per local day, and a segment ending at midnight ends at `time.max`.** Converting an instant-plus-duration into `availability_exceptions` rows splits at midnight in the mentor's own zone | `exception_window_ordered` requires `end_time > start_time`, and there is no `24:00` to write — so a session crossing local midnight cannot be one row, and an unsplit write fails with a `500` on a cancellation the mentor is entitled to make. **It is reachable rather than theoretical**: a declared window cannot cross midnight, but that CHECK is in the *rule's* timezone and nothing requires a rule's zone to be the mentor's own. `time.max` leaves the final microsecond of a day unblocked, which no slot can occupy; blocking the whole following day instead is a real cost to a mentor. The end is computed in UTC and converted, never by adding a `timedelta` to a local wall clock, which drifts an hour across a transition | Never |
| 156 | **A third-party grant is probed on a schedule, and the probe is the narrowest question the provider will answer.** Confirmed-working grants are stamped and skipped for twelve hours; a fatal failure is recorded and the owner told **once**; a transient one changes nothing at all - not marked, not stamped, not counted as checked | Revocation is silent: a provider whose access you lose sends nothing, so the only way to find out is to ask. ADR 0004 named the absence of this as a live gap and said it is *"precisely how this situation went unnoticed"*. **Reactive detection is not enough** - it needs somebody to visit the surface that uses the grant, so the account nobody browses is the one that fails quietly, which is the case that actually bit. **Leaving a transient failure unstamped is what makes the distinction work**: stamping on a failed probe would read an outage as twelve hours of confirmed health, and marking on it would turn a rate limit into a re-consent. The staleness filter rather than a row limit, because a limit leaves the same accounts at the back of the queue every run | The provider gains a revocation webhook, which replaces the polling half and not the recording half |
| 157 | **A guard that another guard already covers still needs a test at its own level.** Two mutation survivors here were both real guards whose removal changed nothing, because a second check downstream caught it | Both looked covered: a test exercised the path and passed either way. `record_failure`'s already-dead check is unreachable through the sweep - a dead grant is not selected again - but load-bearing for the concurrent read path; `connections_due`'s status filter is redundant for correctness and load-bearing for cost. **A test that passes with the guard removed is testing the other guard**, and the docstring claiming it covered both was wrong. Assert the unit, not only the path through it | Never |
| 158 | **A message about a state change is queued only when the state actually changed, and in the same transaction as the change.** Read the prior value before writing, compare, and enqueue on the difference | An endpoint that permits a repeat — and this one should, because an append-only decision log records what admins did rather than what is true — will get one from a double-click. The write is idempotent and an email is not. Same transaction in both directions: a message about a write that failed is worse than silence, and a write with no message is the gap this row exists to close. **Reading the prior value has to happen first**, because `trg_apply_mentor_status` projects the event onto the column, so after the write the column already says what was just decided | Never |
| 168 | **A `CHECK` does not guard a value the column silently coerces on the way in.** A range constraint proves the *stored* number is in range; it says nothing about whether that number is the one the source carried. Where a transform narrows a source value into a tighter type, **the mapping is the guard and the constraint is only the backstop** | Measured on `reviews`: the legacy export stores a three-point scale as `1.67 / 3.34 / 5`, and `3.34` assigned to a `smallint` **rounds to 3** and satisfies `BETWEEN 1 AND 3`. A loader that casts instead of mapping stores *Excellent* where the mentee chose *Great* — every row loaded, every gate green, and nothing anywhere saying the data is wrong. The plan's own wording, "the load would fail or truncate silently", allowed for either; it is always the second, because PostgreSQL's assignment cast rounds rather than refuses. The shape is wider than numerics — `varchar(n)` truncates, a narrower timestamp loses precision, and each passes its own constraint afterwards | Never. What the constraint *is* for stands: an unmapped third value fails loudly at rehearsal, which is why the bound ships tight |
| 169 | **An application-level pre-check races, so the constraint answers in the same shape the check does.** Where a rule is enforced by both a `SELECT` and a unique index, the writer catches the integrity error *by constraint name*, rolls back, and raises the same domain error the check would have | Two taps on a bad connection both pass the `EXISTS` before either commits — nothing is visible until it is — so the second insert blocks on the index and fails when the first commits. An `IntegrityError` is not an `AppError`, so nothing maps it: the caller gets a `500` for a request the product has a clean answer to, and the endpoints most likely to be double-tapped are the ones a phone retries. `book_session` established the shape and `write_review` is the second instance, which is what makes it a rule rather than a fix. **Matched by name, not loosely**: a different integrity failure re-raised as a duplicate would report the wrong cause with total confidence. **Rolled back inside the writer**, because the transaction is already aborted and every later statement would fail with `InFailedSQLTransaction`, burying the real cause under it. The interleaving is testable rather than hypothetical — poll `pg_stat_activity` for a lock wait, never sleep a plausible interval | Never while both layers enforce it. A rule enforced *only* by the constraint needs no pre-check, and one enforced only by the check has a hole |
| 160 | **A provider template declares its own merge fields; the code holds one resolver per field *name*, never a field list per template.** Every declared name resolves or the send fails naming it, and an empty declaration means *unpublished* rather than *needs nothing* | Nine templates times ten fields is ninety slots and about twenty distinct names, so a registry keyed by name is a quarter of the work and shares by construction. It also makes renaming the provider's templates optional — aliases carry two disagreeing generations onto one resolver — which removes an editing pass that would otherwise block the first send. **The assertion is the point, not the typing**: a provider accepts a missing key and renders it as nothing, so an unresolved name is a delivered email with a hole in it, where a refusal is a failed row somebody can see. This deliberately inverts who owns the contract — whoever edits a template controls what it requires — and the cost is that a template edit can break sends, loudly | The provider stops publishing its declarations, which puts the field lists back in code |
| 162 | **A uniqueness rule over an outbox row includes the recipient**, because the table stores one row per person on purpose | `uq_outbox_events_reminder` was `(entity_id, event_type, kind)` and correct — the only reminder went to one person. The moment a reminder went to both parties the second row conflicted and `ON CONFLICT DO NOTHING` dropped it in silence: one party reminded, the other not, and nothing anywhere recording that. The row-per-recipient shape exists so a send failing for one is retried for that one, and any index blind to the recipient collapses that back. **Assert an expression index against `pg_indexes`**: `alembic check` says outright that it cannot compare one, so a model and its migration can disagree with every gate green | Never |
| 164 | **A message to a *role* resolves live grants at send time and excludes the grant that cannot act on it.** One outbox row per person, and an empty set is a legitimate answer rather than an error | Every other message here names its recipient on a row; this asks who currently holds a grant, which is a different question and the reason it does not go through the session audience table. **Live**, because `admin_users` exists to make revocation mean something and an alert reaching a revoked admin is the first place that stops being true. **Excluding `limited_access`**, which may look and not act, because a message about a queue you cannot clear has no possible response but to find somebody else. **Empty is not an error**: a deployment with no admins still accepts applications, and refusing one because nobody could be told is the wrong way round | Roles gain a notification preference, which is a per-person setting rather than a property of the grant |
| 165 | **Daily enforces both gates: a private room refuses an untokened visitor, and `nbf` refuses an early one.** Measured in a browser, two distinct refusals. So a Daily session is protected twice — link withheld until the join window *and* the door shut — where a Meet session is protected once, because a Meet link admits anybody holding it at any time | This was the fork the spike existed for. Enforced means early joining is *impossible* on Daily and merely *discouraged* on Meet, so **the two venues cannot share UI copy** — telling a mentor "nobody can join early" is true of one and wishful about the other. It also makes the privacy guarantee sayable: a promise nobody checked is the kind that surfaces in a complaint. Both `nbf`s are set together, on room and token, so which one refuses is untested and does not matter while both are set | Daily changes either behaviour, which would be a defect to raise with them rather than a design to revisit |
| 166 | **A docstring that says something is unbuilt is a claim with an expiry date, and nothing enforces it.** When you build the thing, correct the text that said you had not — in the same change | Seven of these were found in one cycle, five of them by accident. Two were published in the OpenAPI spec, so a client reading the contract was told the opposite of what the code does. This codebase writes *why* in its docstrings, which is what makes them worth reading and exactly what makes them go stale. **There is no gate to add**: a sweep for the obvious phrases runs at roughly 1-in-20 signal because most hits are "does not exist" describing a 404, and one near-miss — two same-named columns on different tables, one read and one not — would have become a wrong correction if the grep had been trusted. The only thing that catches these is checking the claim while touching the code it describes | A docstring linter appears that can resolve a claim against the code. None exists |
| 167 | **One message per thing being asked.** A post-session *platform survey* to both parties and a *mentor review* from the mentee happen at the same moment and are not the same message | They differ in audience, in subject and in which phase owns them, and a single member merging them is wrong in a way that is invisible until somebody reads the email: asking a mentor to review the session tells one person too many, and asking a mentee about the platform when you wanted a review gets you the wrong answer. Found because two sessions independently designed `SESSION_FEEDBACK` and `REVIEW_REQUESTED` for the same slot in the lifecycle. The built one was **withdrawn rather than left unproduced** — #21 does not carry a vocabulary member for a feature nobody is building, and a dead member is what the next person wires by mistake | The platform survey is actually wanted, which makes it a second message beside the review rather than a rename of it |

Anything conflicting with a row here is an **ADR**, not an implementation detail.

## Domain vocabulary

> **Two rows are still open, and the gaps are load-bearing.** If a requirement uses
> one of those words, **ask**. Do not resolve it toward whichever reading is easier
> to build.

| Term | Means precisely | Does **not** mean |
|---|---|---|
| **Mentee** | The student receiving mentorship | Not "user" — mentors are users too |
| **Mentor** | The person providing mentorship, typically at an institution abroad | **There is no separate "coach".** "Mentor/coach" in product copy is one role, and the codebase uses `mentor` throughout |
| **Availability** | A mentor-declared window in which they *can* be booked | Not a session. An availability with no booking is not an event that happened |
| **Booking** | The *act* of a mentee claiming a slot. It creates a `sessions` row (ADR 0007) | **Not a separate entity.** There is no `bookings` table — the legacy `SessionBooking`/`SessionTracker` split was a Bubble workaround, not a domain distinction |
| **Session** | The `sessions` row itself, from the moment it is claimed through whatever it becomes | **Not "mentorship that took place"** — that is `status = 'completed'`. A cancelled session is still a session row, still counted, and is never soft-deleted |
| **Service offering** | *What kind of help* — a row of the closed six-item taxonomy in `service_offerings`, owned by the platform and shared by everyone. "Document Review." Both `mentee_goal_needs` and `mentor_service_offerings` point at it, so **this is the matching axis**: it is the join that puts a mentee in front of the right mentor | **Not a thing you book**, and not a mentor's own creation. Users may never add a top-level one (#53) — free text turns "SOP help", "Statement of Purpose" and "sop" into three rows that match nothing |
| **Session type** | *The bookable thing* — one mentor's own product, with a name, and a `session_type_booking_configs` row giving its duration, notice and venue. "SOP review, 45 minutes, on Zoom." A mentor may hold several; `sessions.session_type_id` records which one was booked | **Not a service offering, and never called a "service".** The word `service` is already spent on the taxonomy above, and putting it on this concept too would place two `service_*` meanings in one schema — the conflation #53 exists to prevent, arriving through vocabulary instead of data. Called **session type** in the schema, the API and the UI; Calendly and Cal.com both say "event type" on screen and nobody is confused |
| **Party** | One of the **two people a session is between**, read off `sessions.mentor_id` and `mentee_id`. Always exactly two, always present — both columns are `NOT NULL` with foreign keys. `PartyRead` is the public shape | **Not a participant.** A party is who the session *is with*; it exists the moment the session does, whether or not anybody turned up |
| **Participant** | A `session_participants` row — who actually **attended**, with `role`, `joined_at`, `left_at` and `attendance_status` | **Not a party.** This is attendance, not identity, and it is the table group sessions will read from once a session is between more than two people. Until then the two words describe the same humans and different facts, which is exactly when they get used interchangeably and stop meaning anything |
| **Bookable** | A mentor is **set up** to take bookings: approved, listed, and holding a live offering with a duration plus a live availability rule. It is what `GET /mentors` filters on, and what `mentor_is_bookable()` expresses | **Not available.** It says nothing about *when*. A mentor booked solid for a month is bookable and has no free slot for a month |
| **Available** | A specific span of a specific mentor's time that a specific offering could be booked into — computed per request by `bookable()` in `domain/availability.py`, never stored (D18) | **Not a column and never one.** `availability_rules` holds what a mentor *declared*, which is one input; the others are existing sessions, dated exceptions and the notice window. A stored `is_available` is the drift D20 rejected — wrong the moment somebody books |
| **Rate** | What a mentor charges, set by the mentor | Not the pricing guide |
| **Pricing guide** | A platform-computed *suggestion* derived from the mentor's experience and profile | **Not a price.** Advisory only, never persisted as the agreed amount |
| **`legacy_bubble_id`** | The Bubble `_id` a row came from. ADR 0002 called it `legacy_id`; renamed by ADR 0007 | Not our primary key, and never exposed in an API |
| **Port** | An interface `domain/` owns — a Protocol in `domain/ports.py` — implemented by an adapter in `infra/` and wired in `api/deps.py`. Swapping a vendor is a new adapter class and one wiring line | Not a network port. Not a thin wrapper around a vendor SDK either — if the vendor's vocabulary shows through the Protocol, `domain/` is still coupled to it |
| **`institutions.domain`** | The hipolabs domain for an institution — what "which university" resolves to, and the natural key rows are upserted and deduplicated on. Null only for `source='manual'`, which is why it is not the primary key (ADR 0008) | Not the primary key — that is the surrogate `uuid`, and it is what another row points at. Not a university name either: a name is a display string. **There is no `ror_id`**; ADR 0008 supersedes the settled decision that defined one |
| **`whatsapp_conversation_id`** | Zernio's WhatsApp thread identifier, stable per participant and sending account, stored on the user record. Renamed by ADR 0007 — a vendor identifier carries the vendor's name | Not a message id. Unrelated to our own `conversation_id`, which is the primary key of our booking-scoped message threads (ADR 0006) and lives in our database |
| **`slug`** | The public profile handle on `users` — `sakiratu-adeleke`. Lowercase, digits and hyphens, unique among live users, and the thing a profile URL is built from. Carried over from Bubble, where 39 of 43 dev users have one | **Never an identifier to pass between layers, and never an authorization key.** It is user-visible, it is mutable, and it is globally unique in a system that has deliberately deferred multi-tenancy (#28). The primary key is `users.id`, which is the Supabase auth user id |

**Session shape.** Sessions may be 1:1 **or group**. Group is a new capability
that does not exist in the legacy app — **every legacy record is 1:1**, so the
migration never encounters a group session and the importer does not need to
handle one.

**1:1 is the product and comes first.** Group is a later feature, and building
for it now would shape the core around a case that does not exist yet. When it
arrives, the mentor supplies the number of participants they want; capacity is a
value a user chooses, not one the platform derives.

**Group adds an invariant rather than replacing one.** A mentor still cannot be
in two sessions at once, and a group session is still one row, one mentor, one
window — so the `EXCLUDE` on `(mentor_id, window)` stays correct and unchanged.
What is added is a count of `session_participants` against that capacity. The two
read as one rule today only because, while every session is 1:1, "the mentor is
busy" and "the slot is taken" are the same sentence.

**Still open — do not guess:**

- **Does a mentee pay per session, per package, or by subscription?** Deliberately
  undecided while the model is explored. Do not hard-code any one of them. The
  shape that keeps all three open is to separate what was *purchased* from what
  was *booked*, so a new model is a new purchase type rather than a migration.
- **What is the cancellation and refund window, and who absorbs the fee?**
  Deliberately undecided. Express it as a parameterised policy in `domain/`, not
  as constants in a handler, so changing it changes parameters.

Neither blocks the initial build: **payments are out of scope until the core
product backend and the migration are done.**

## House conventions

- **Configuration:** everything through `core/config.py`. Only `ENVIRONMENT` and
  `DEBUG` are `EDUFURTHER_`-prefixed; the rest name their own subject.
  No inline `os.environ` anywhere else. Unrecognised prefixed variables fail at
  startup — `extra="forbid"` does **not** do this on its own, which is why there
  is an explicit validator.
- **Secrets:** `SecretStr`, never logged, never in a response body. In a committed
  `.mcp.json` or similar, referenced as `${ENV_VAR}` and never inline.
- **Identifiers:** three distinct spaces, never interchangeable — our internal
  primary key, the `legacy_bubble_id` from Bubble, and any provider-side id (payment
  intent, payout). Translate at the boundary; do not compare across spaces.
- **Money** *(when payments arrive — not in the initial build)*: integer minor
  units plus currency, never a float. The ledger is append-only; corrections are
  new entries, never `UPDATE`s.
- **Vendor SDKs:** may be imported only inside `infra/`. `domain/` expresses the
  need as a Protocol in `domain/ports.py`.
- **Errors:** subclasses of `core.errors.AppError`, which is transport-agnostic.
  HTTP status codes are chosen in `api/`, never raised from `domain/`.
- **Not-found vs not-yours:** both raise `NotFoundError`. Distinguishing them
  leaks the existence of other people's rows to anyone who can enumerate ids.
- **Error envelope and pagination:** *not yet defined.* Decide both before the
  second endpoint exists, not the tenth — the Next.js client will encode whatever
  shape ships first.

## Guardrails

Every build preserves these, whatever it is doing. They become the "guardrails"
section of each Definition of Done.

- [ ] Object-level authorization is scoped **in the query**, on every read *and*
      write path — never checked after fetching
- [ ] No vendor SDK imported outside `infra/`
- [ ] **A new dependency's import name is added to
      `[tool.check-layers.forbidden-external]` in the same change.** The list is a
      denylist — default-allow — so an unlisted vendor is unguarded, and a vendor
      that arrives as a *transitive* of an extra is unguarded and unannounced. The
      guard also skips `main.py` and `api/deps.py`, which is where wiring lives, so
      it prevents an accidental import rather than a deliberate one
- [ ] Secrets never reach a log line, a response body, or git
- [ ] Bubble snapshots stay out of git — `data/`, `exports/`, `*.csv` are ignored
      because they hold 1,200 users' PII, plus 858 personal-info and 940
      education rows, which `gitleaks` does not scan for. It finds credentials,
      not people. **The extract now lands in a `staging` schema rather than in
      files** (ADR 0007), so the same data sits inside the Supabase project from
      ADR 0005 — out of git is still necessary and no longer sufficient
- [ ] **Credential fields are dropped at extraction, before anything is
      written** — `calAccessToken`, `calRefreshToken` and their expiry columns.
      **They are expired Cal.com tokens from the abandoned integration, not live
      Google credentials**: decoded, both access tokens expired over 400 days ago
      and both refresh tokens over 50. Three records asserted otherwise, inferred
      from the field names. Redaction stays because it costs a field list and
      makes the snapshots boring, not because there is an incident. `composioAuthId`
      is **not** redacted — it is a vendor reference, not a token, and it migrates
- [ ] Every table derived from its own Bubble Thing has `legacy_bubble_id`, and
      importers are idempotent on it. **Narrowed by decision #27** — a table
      derived from *columns of* a parent Thing anchors on the parent's foreign
      key instead, because it has no Bubble id of its own and a column null on
      every row anchors nothing
- [ ] Overbooking is prevented by a **database constraint**, not an
      application-level check-then-insert. Legacy did the check-then-insert, and
      **mostly in the frontend** — so it could be bypassed by any path that did
      not go through that screen, and could not see a second person clicking at
      the same moment. The constraint moves the control from the browser to the
      database, which is as far as it can move.
      **Two invariants, not one, and only the first exists today:**
      *a mentor cannot be in two sessions at once* — an `EXCLUDE` on
      `(mentor_id, window)`, which **group sessions do not change**, because a
      group session is still one row, one mentor, one window; and *a session
      cannot hold more attendees than it offers* — a count of
      `session_participants` against a mentor-supplied capacity, which does not
      exist yet and ships with group sessions. Earlier wording said group made
      the invariant "capacity rather than exclusivity"; it makes it capacity **as
      well as** exclusivity, and reading it the other way would retire a
      constraint that stays correct
- [ ] Times stored UTC, with the mentor's IANA zone as a separate column
- [ ] *(once payments exist)* Money is integer minor units with a currency; no
      float touches an amount
- [ ] **Every table added carries `id uuid PRIMARY KEY DEFAULT uuid_generate_v7()`**, and any invariant a natural or composite key would have carried is re-declared as `UNIQUE`. Losing that constraint is silent — duplicates become legal and nothing surfaces it until somebody reads the wrong row
- [ ] **A list endpoint's query plan is asserted with `EXPLAIN`, not assumed.** No
      sequential scan, at a row count the table will actually reach. Added after a
      plan read off the index definitions was wrong in the direction that had
      already shaped an API contract — `?role=` was going to be *required* to keep
      a list indexable that was never unindexed. A plan depends on statistics and
      row counts, which no amount of schema-reading contains
- [ ] No threshold lowered to go green

## Commands

```bash
uv sync --all-extras --dev                              # install
uv run pre-commit install                               # hooks (pre-commit + commit-msg)
cp .env.example .env                                    # local settings
docker compose up -d                                    # Postgres 17 on 55432
uv run alembic upgrade head                             # migrations (never on startup)
uv run python scripts/check.py --fast                    # the inner loop, ~20s
uv run uvicorn app.main:app --reload --app-dir src      # run
uv run pytest                                           # tests
make check                                              # the full local gate
uv run python scripts/check.py                          # the same gate, without make
```

Tests marked `db` read `TEST_DATABASE_URL` and **skip** without it, so the suite
runs on a machine with no Docker. CI sets `REQUIRE_DB_TESTS=1`, which turns that
skip into a failure.

## Where the truth lives

| Doc | Covers | Canonical copy |
|---|---|---|
| `CLAUDE.md` | Router; the few always-true facts | repo root |
| This file | Settled decisions, vocabulary, guardrails | `.claude/skills/project-conventions/` |
| `docs/adr/` | Decisions and their rationale | repo root |
| `references/failure-modes.md` | What has actually gone wrong here | alongside this file |
| `docs/bubble-data-model.md` | Legacy Bubble shape, fields, and row counts | repo root |
| `docs/edufurther-migration/` | The **target** schema, field mapping and runbook | repo root — **received, never edited here** (ADR 0007) |
| `README.md` | Human-facing setup and layout | repo root |

**Canonical copy rule.** Where a doc is duplicated, **name the one that wins** and
edit only that. Duplicated facts drift, and the copy you forget becomes wrong.

## Enforcement blind spots

State a gate's blind spots next to its coverage, every time.

- **Nothing server-side *prevents* a bad merge; one thing *detects* it.** CI runs
  on pull requests, `no-commit-to-branch` blocks local commits to `main`, and
  `main-guard.yml` fails when a commit reaches `main` without an associated pull
  request. That last one runs *after* the push has landed — it turns a silent
  bypass into a red check, and it cannot stop anything. Treating detection as
  equivalent to prevention is how a gap gets closed on paper and stays open in
  fact. Real prevention needs a GitHub ruleset, and both the classic
  branch-protection and rulesets APIs return 403 on a private repo on the Free
  plan — rulesets are not a free workaround. The choice between GitHub Pro and
  making the repo public is issue #9. There is still no CODEOWNERS file despite
  `.pre-commit-config.yaml` citing one.
- **There is no dataflow analysis.** CodeQL is absent from the Security workflow
  because uploading results needs GitHub Advanced Security, which a private
  personal repo does not have. bandit, ruff's `S` ruleset and pip-audit cover
  pattern-matched issues and known CVEs; **nothing tracks tainted input across
  function boundaries.** Restore by enabling GHAS or making the repo public.
- **`gitleaks` finds credentials, not PII.** It will not stop a member export.
- **The layer check reads imports, not behaviour.** It cannot see a domain rule
  implemented in `api/`.
- **The suite tests the application over ASGI; `tests/e2e/` tests the protocol,
  and only for the request body.** `httpx.ASGITransport` speaks ASGI directly and
  never implements HTTP — no chunked encoding, no `Content-Length` framing, no
  connection to drop. That gap cost this project both directions inside one pull
  request: it reported a lying `Content-Length` as a hole that does not exist,
  and it hid a chunked request that bypassed the body ceiling entirely.

  `tests/e2e/` now closes **the body-ceiling half** with a real uvicorn server —
  chunked, declared, lying, disconnected. Everything else about HTTP is still
  untested: keep-alive, pipelining, header limits, TLS, timeouts, and the
  behaviour of any route under a real client. Saying which half is closed matters
  more than saying the gap is gone.
- **`alembic check` cannot see triggers, and one now carries a rule.**
  `trg_apply_mentor_status` is what keeps `mentor_profiles.approval_status` and
  `.listing_status` in step with `mentor_status_events`; nothing in the gate
  compares it against anything. `test_mentor_status_log` inserts an event
  **directly** and asserts the column moved — which is the only thing that
  distinguishes a working trigger from a caller that happened to write both, and
  the first version of that trigger did not work at all.
- **`alembic check` cannot compare an expression index carrying an operator
  class.** It says so and skips: *"Cannot compare index
  'ix_institutions_name_prefix' ... detected as including operator clause"*. So
  model-versus-migration drift on `lower(name) text_pattern_ops` — which is what
  makes institution autocomplete 4 ms rather than 59 ms — is invisible to the
  gate. Two tests cover it instead: the index is declared on the model, and
  present in the database with both halves of its definition.
- **`alembic check` sees tables, columns, types and regular indexes — and nothing
  else.** It is blind to functions, triggers, partial indexes, `CHECK` and
  exclusion constraints, and to any model never imported into `env.py`. That is
  most of what makes this schema correct: the double-booking exclusion
  constraint, the soft-delete partial unique indexes, `set_updated_at`. A green
  migration check means the chain applies, not that it is complete.
- **`alembic check` compares foreign keys by their column signature, never by
  their name.** So a constraint whose name in the database differs from the name
  in the source file is invisible to it, and to everything else in the gate —
  nothing reads constraint names at all. That is how a 65-character key shipped
  under a truncated, hashed name that appeared nowhere in the repository, with
  six green CI checks and a thirteen-way mutation batch. `test_no_declared_
  identifier_exceeds_the_postgresql_limit` closes the length case at declaration
  time; **a name written directly into a migration and into no model is still
  unguarded**, because the test walks `Base.metadata` and a migration is a
  separate artefact.
- **Nothing compares the Alembic chain against `docs/edufurther-migration/`.**
  ADR 0007 names this and ADR 0011 does not fix it. The package is canonical for
  what the target *should* contain, the chain for what a database *does*, and
  when they drift nothing says so. Mitigated only by the rule that a migration
  departing from the package states the departure in the migration.
- **Neither bandit nor mypy sees `migrations/`.** `scripts/check.py` runs
  `bandit -r src` and `mypy src`, and the pre-commit mypy hook is scoped
  `files: ^src/`. Migrations live at the repo root — deliberately, so they are
  not packaged into the wheel — which means the hand-written raw SQL most likely
  to carry an injection or a typo is checked by ruff alone. `alembic.ini`'s
  post-write hooks keep it formatted and linted; nothing type-checks or
  security-scans it.
- **A `db`-marked test that skips looks exactly like one that passed.**
  `REQUIRE_DB_TESTS=1` in CI converts the skip to a failure, which is the only
  reason the database tier cannot silently disappear. Locally the skip is
  correct — not every machine runs Docker — so the protection exists in exactly
  one place and is one environment variable deep.
- **Nothing detects duplication, and the copies that matter are invisible in
  principle.** No gate step measures it. Worse, the two shapes that have
  actually cost this project are ones no static tool could see: a predicate
  inside a `text()` string is not a symbol mypy or ruff can bind, and a test
  double is just more test code. `test_provisioning_store.py` closes exactly
  one instance — statements in the provisioning store that filter `users` —
  by walking the module rather than a declared list, so a new statement cannot
  quietly go unchecked. **Nothing generalises that to the next module.** It is
  review against decision 43, and that is the whole of the enforcement.
- **Nothing stops an ordinary `UPDATE` from destroying a migrated timestamp.**
  `users.updated_at` carries Bubble's Modified Date, and `trg_set_updated_at`
  fires on every update, so any statement that touches a migrated row rewrites
  it to the import clock — silently, with nothing to restore it from. The ETL
  and the provisioning CLI both hold the trigger off via
  `infra/etl/loader.timestamps_from_source`; **nothing makes the next writer do
  the same.** It is caught only by a per-caller test asserting a known past
  timestamp survives, and only where somebody thought to write one.
- **Bubble field completeness cannot be fully automated** — the authority for
  "what fields exist" is the Bubble editor UI.

## Failure modes

`references/failure-modes.md` is this project's incident log. **Read it before any
non-trivial build, and add a row whenever something is found the hard way** —
including near-misses.

It is the most valuable thing in this overlay. Generic standards encode what
usually goes wrong; that file encodes what has actually gone wrong *here*.
